# tamp — Quality & Edge-Case Audit (2026-06-16)

Branch `converted-tree`. 77 confirmed findings, 1 refuted. Each finding was surfaced by a per-area senior-QA agent reading the real code and adversarially verified against the source. Severity: **S1** critical/data-loss/crash · **S2** high (common failure / major UX / a11y blocker) · **S3** medium (polish / uncommon edge) · **S4** low (nice-to-have). `~` = plausible (state-dependent).

## Functionality & correctness

### [S2] Dropping videos on the Converted or Preferences tab opens an invisible quick-pick that swallows keys and resurfaces later

- **Evidence:** src/lib/dragdrop.ts:38-54 registers a global webview drag-drop listener and calls deps.compressPaths on any drop regardless of active tab. main.ts:97-100 wires that to listView.compressPaths. In quick-pick mode (the default), src/views/list.ts:765-773 -> openQuickPickForPaths (line 669-733) does `el.appendChild(overlay)` where `el` is the list view root. main.ts:74-77 setTab() sets `listView.el.hidden = tab !== "videos"`, and styles.css:83-85 `[hidden]{display:none!important}` hides all descendants, so the appended .quickpick-overlay is never visible. Meanwhile list.ts:374-405 onKeyDown now has `quickPick` set and swallows ALL keys (including arrows/Enter) even though the user sees nothing. setTab() never calls closeQuickPick(), so when the user returns to Videos the stale overlay (for files dropped minutes earlier on another tab) suddenly appears.
- **Impact:** A drag-and-drop onto the panel while on Converted/Preferences appears to do nothing (the drop hint overlay even shows on body, encouraging the drop), the keyboard goes dead until Escape, and a phantom preset picker pops up later when switching back to Videos â€” applying to whatever files were dropped earlier. Feels broken.
- **Proposed fix:** Either force the active tab to Videos before opening the picker (call setTab('videos') in compressPaths when a drop arrives), or append the quickpick overlay to the always-visible .panel host (like the drawer/custom modal do) rather than the per-tab hidden el. Also call closeQuickPick() on tab switch.
- **Decision needed:** When a user drops files while on a non-Videos tab, should tamp switch them to the Videos tab to show the picker, or silently process with the default preset?
- **Verifier:** Real and reproducible. dragdrop.ts:38-54 registers an unconditional global webview drop handler -> main.ts:98 listView.compressPaths; default layout is quick-pick (list.ts:564 default, shouldPickPreset line 59) so compressPaths (765) calls openQuickPickForPaths, which does el.appendChild(overlay) (730) onto the list root that setTab (main.ts:75) sets hidden when not on Videos, and styles.css:83 [hidden]{display:none!important} makes the overlay invisible. onKeyDown is on document (list.ts:473) and its `if(quickPick)` swallow (374-404) runs BEFORE `if(el.hidden) return` (406), so all keys are eaten cross-tab. setTab never closes it (close only happens in onSettingsChanged:1191; exported API 1208-1217 has no closeQuickPick), so returning to Videos un-hides the stale picker. The body-level drop-overlay (dragdrop.ts:29) even shows the drop hint on other tabs. Escape (377) does still close it, so the keyboard lock is escapable but invisible.

### [S3] A background conversion finishing while you browse the Converted tab snaps your selection to the top and collapses open groups

- **Evidence:** main.ts:106-113 onEncodeState refreshes the Converted view on every 'done' event when its tab is visible. src/views/converted.ts:351-373 refresh() does `scroll.innerHTML = ""` then rebuilds all rows and unconditionally `selEl = null; setSelected(navRows()[0])` (line 371-372), discarding the user's keyboard selection and any expanded split group.
- **Impact:** If you're keyboard-navigating or have a split group expanded on the Converted tab and any queued/running conversion completes, the list jumps to the top and your group snaps shut underneath you. Jarring during active use.
- **Proposed fix:** Preserve the selected row identity and expanded-group state across refresh (keyed by output path/folder), or do an incremental update that only prepends the new record instead of a full rebuild.
- **Decision needed:** Should the Converted tab auto-refresh on completion at all while the user is interacting with it, or only refresh on next tab entry / via a subtle 'new items' affordance?
- **Verifier:** main.ts:110-112 refreshes the Converted view on every per-job 'done' event (ipc.ts:105 â€” fires for queued/background jobs too) when the tab is visible; converted.ts:359 wipes scroll.innerHTML and 371-372 unconditionally resets selEl=null + setSelected(navRows()[0]) (scrollIntoView at 310 snaps to top), and groups rebuild collapsed (children.hidden=true at 279, no is-open class). Open-group state lives only in DOM (is-open, 282-284), so it's destroyed. No selection/scroll/expansion preservation anywhere. Reproducible exactly as described.

### [S3] Row '1-9 quick profile' shortcut and the expanded-row chips disagree on which preset is number 1

- **Evidence:** src/views/list.ts:454-459: the selected-row digit shortcut indexes orderedPresets() (default-first, list.ts:568-574). But the expanded-row chips at list.ts:927-943 iterate settings.presets in the user's ORIGINAL order. The quick-pick overlay also uses orderedPresets() (line 672). So with a default preset that isn't first in the user's list, pressing '1' on a selected row converts with the default, while the first visible chip in the same row's expanded panel is a different preset.
- **Impact:** Inconsistent mental model: the same '1' means two different presets depending on whether you used the keyboard shortcut or read the chip order. A discerning user notices their '1' key and their first chip do different things.
- **Proposed fix:** Pick one canonical order. Either render the expanded-row chips in orderedPresets() order (default-first, matching the keyboard and quick-pick), or number the chips 1-9 explicitly so the digit binding visibly matches.
- **Decision needed:** Should preset numbering everywhere be default-first, or should it follow the user's preset list order (and the default just be marked)?
- **Verifier:** Real: digit shortcut uses orderedPresets() (default-first, list.ts:567-574 -> 455/459) while expanded chips iterate settings.presets in original order (list.ts:928); when default isn't user's first preset, '1' and the first chip target different presets. S3 fits â€” chips carry no visible digit labels so the clash is subtle, not a hard contradiction shown on screen.

### [S3] Split jobs append one journal record per part, so a single split eats up to 20 of the 200 history slots and can render half-evicted groups

- **Evidence:** src-tauri/src/journal.rs:14 caps the journal at MAX_RECORDS=200 (drain oldest on append, line 86-89). src-tauri/src/encoder/mod.rs:830 calls append_journal INSIDE the per-part loop in encode_part_set, so an N-part split writes N records. src/lib/convgroup.ts:37-58 reconstructs a group purely from whatever part records are present; if eviction removes some parts of an older split, groupConversions() builds a group with fewer parts, a wrong `${node.parts.length} parts` badge (converted.ts:261-262) and an understated totalBytes.
- **Impact:** Users who rely on splitting churn through their Converted history far faster than the '200 conversions' the cap implies (a static-20 split = 20 slots), evicting unrelated single-output history. A group straddling the eviction boundary shows a wrong part count and total size.
- **Proposed fix:** Record a split as one logical journal entry (store the part list/folder in a single record), or count the cap in logical conversions rather than rows; at minimum, when grouping, drop or flag groups whose part set looks truncated.
- **Decision needed:** Should the journal cap be 200 logical conversions (splits counting once) rather than 200 rows, and should a multi-part split be one record?
- **Verifier:** append_journal is inside encode_part_set's per-part loop (encoder/mod.rs:830), writing N records/split; journal.rs:86-89 drains oldest beyond MAX_RECORDS=200; plan.rs:276 clamps parts to 20 (test l.1500), so a split can consume 20/200 slots. convgroup.ts:48-55 rebuilds a group from only the present part records with no completeness check, so a straddled-eviction group renders an understated totalBytes (l.54) and a wrong `parts.length parts` badge (converted.ts:261-262). Cosmetic/history-only (on-disk parts untouched), uncommon, non-destructive â€” S3 fair.

### [S4] Converted-tab time tooltip is appended to <body> on hover and can leak if the row is removed mid-hover

- **Evidence:** src/views/converted.ts:131-153 timeEl() attaches the tip element to document.body on mouseenter (line 150) and removes it on the matching mouseleave (line 152). refresh() (line 359) does `scroll.innerHTML = ""`, which removes the row WITHOUT firing mouseleave. If a background conversion completes (main.ts:110) while the tooltip is showing, the row is destroyed but its body-attached tip stays in the DOM permanently, and its mouseleave handler can never run.
- **Impact:** A stray Created/Converted popover can be left floating over the panel after an auto-refresh that happens while hovering a time value; repeated occurrences accumulate orphaned nodes.
- **Proposed fix:** Track the active tip and remove it in refresh()/on row teardown, or render the tooltip as a child positioned via CSS rather than a body-attached node, or clean up on a global pointer move.
- **Decision needed:** Is the hover tooltip worth the body-attachment complexity, or should the Created/Converted detail be shown inline (e.g., title attribute) to avoid lifecycle leaks?
- **Verifier:** converted.ts:150 appends per-row `tip` to document.body on mouseenter, removed only by the mouseleave handler (line 152); refresh() at line 359 does `scroll.innerHTML = ""`, which destroys the row `wrap` but not the body-attached `tip`, so no mouseleave fires and the tip leaks. main.ts:110-111 auto-refreshes on `state.phase === "done"` while the Converted tab is visible â€” a non-user-driven mid-hover refresh â€” so the orphan-popover scenario is real. Cosmetic-only; S4 is correct.

### [S4] Quick-pick 'Customâ€¦' and per-row Custom are unavailable for dropped/added files outside the watched folders

- **Evidence:** src/views/list.ts:707-722 only adds the Custom button when `videos.find(x => x.path === paths[0])` matches a recents entry; a file dropped from outside the watched folders isn't in `videos`, so single-file external drops get no Custom option. The keyboard 'c' branch (list.ts:389-396) has the same gate. compressPaths/doEnqueuePath (list.ts:535-559, 765-773) handle external paths for presets only.
- **Impact:** Dropping a one-off video from Downloads and wanting a custom one-off conversion isn't possible from the picker â€” Custom is silently missing â€” even though Custom is conceptually the ideal path for an ad-hoc external file.
- **Proposed fix:** Allow Custom for any single path by synthesizing a minimal RecentVideo (name from basename, size via stat) instead of requiring a recents match, since openCustomModal only needs name/path/size.
- **Decision needed:** Should Custom be offered for files outside the watched folders, and if so what metadata (thumbnail/duration) is acceptable to show as pending?
- **Verifier:** Confirmed. Custom is gated on videos.find(x=>x.path===paths[0]) in the quick-pick (list.ts:708-710) and the keyboard 'c' branch (list.ts:392-393). `videos` is populated only from listRecents() (list.ts:235-236), so external files reaching compressPaths via "Add fileâ€¦" (list.ts:116-118) or drag&drop (dragdrop.ts:47-52) are absent from `videos` and get no Custom button/shortcut, while presets still work via doEnqueuePath. S4 fits: real reproducible gap but a niche convenience path, not core breakage.

### [S4] Presets beyond the 9th are clickable in the quick-pick but unreachable by keyboard and show no number

- **Evidence:** src/views/list.ts:694 renders the quick-pick number as `${i < 9 ? i + 1 : ""}` so the 10th+ presets show a blank number badge. The digit handler (list.ts:397-402) only maps keys '1'-'9' (n < presets.length), and presetIndexForDigit (list.ts:64-67) caps at 9 digits. settings.rs:338-371 validate() imposes no upper bound on preset count, so users can create >9 presets.
- **Impact:** A user with 10+ presets sees unnumbered rows in the picker that can only be reached by mouse; the advertised '1-9 apply' has a silent ceiling. Minor but looks unfinished.
- **Proposed fix:** Either cap presets at 9 in the UI/validation, or extend the picker to support paging / 0 as the 10th, and label the overflow rows so it's clear they're mouse-only.
- **Decision needed:** Is there an intended maximum number of presets, and should the picker support more than 9 via keyboard?
- **Verifier:** All cited code matches: list.ts:694 renders blank badge for i>=9; keydown handler list.ts:397-402 only maps '1'-'9'; settings.rs:338-371 validate() has no upper bound on presets.len() (only empty/default/name/target_mb/split/recents_limit checks). 10th+ presets are mouse-only with no number. S4 fits: cosmetic, uncommon (needs 10+ presets), rows still mouse-clickable.

### [S4] Duplicate preset names are allowed and become indistinguishable in every picker

- **Evidence:** src-tauri/src/settings.rs:338-371 validate() checks emptiness/target/split but never rejects two presets with the same name (or the editor creating one). The quick-pick (list.ts:696), active-bar (list.ts:623), expanded chips (list.ts:934) and Converted 'where' label all show only the name. plan.rs:436-462 preset_hash keys reuse on configuration, so two same-named presets with different settings produce two indistinguishable rows; two identical-config same-named presets are functional duplicates.
- **Impact:** A user who duplicates a preset (or mistypes the same name twice) sees two identical-looking rows in the picker with no way to tell them apart, and can't know which the Converted tab's 'Discord (10MB)' label refers to.
- **Proposed fix:** Warn on (or reject) duplicate preset names in validate()/the editor, or disambiguate identical names in pickers with the summary line.
- **Decision needed:** Should duplicate preset names be blocked, auto-suffixed, or just allowed with a disambiguating summary shown?
- **Verifier:** settings.rs validate() (338-371) and editor save (preferences.ts:284-321) check name non-empty/target/split but never enforce name uniqueness; quick-pick (list.ts:696), active-bar (list.ts:623), chips (list.ts:933) and Converted where-label (converted.ts:187,263 -> rec.presetName) show name only. plan.rs:436 preset_hash excludes name/id, so same-named presets are visually indistinguishable. Real but cosmetic-only: ids stay unique (newPresetId), so routing is unambiguous â€” no data loss/crash. S4 correct.

## UX & interaction design

### [S1] Dropping a video while on Converted/Preferences silently freezes the UI (invisible quick-pick)

- **Evidence:** The drop overlay lives on document.body and fires on every tab (dragdrop.ts:29,38-53). A drop calls listView.compressPaths (main.ts:98), which in quick-pick mode runs openQuickPickForPaths -> `el.appendChild(overlay)` (list.ts:730) where `el` is the Videos view. That view is `hidden` when another tab is active, and styles.css:83 forces `[hidden]{display:none !important}`, so the .quickpick-overlay (inset:0 inside `el`, styles.css:1642-1645) never renders. Worse, `quickPick` is now set, so onKeyDown swallows ALL keys before the `if (el.hidden) return` guard (list.ts:374-405 run before line 406). The drop overlay already hid itself (dragdrop.ts:45) and no toast fires.
- **Impact:** A user on the Converted or Preferences tab drags a video onto the panel (the overlay even invites them with 'Drop to pick a preset'), drops it, and nothing visible happens. The keyboard is now dead app-wide (every keystroke is eaten by the hidden picker) until they happen to switch back to Videos â€” looks like a hard freeze. Core 'drop to compress' flow is broken from two of three tabs.
- **Proposed fix:** Mount the quick-pick overlay (and the drop pipeline) on the always-visible `.panel` host like the Custom modal does (custom.ts uses host=.panel), OR have compressPaths/openQuickPick auto-switch to the Videos tab before showing the picker. At minimum, when a drop targets a non-Videos tab, switch to Videos first so the picker is visible.
- **Decision needed:** Should a drop/right-click-compress from any tab force-switch to Videos, or should the picker float above whatever tab is active (panel-level overlay)?
- **Verifier:** Verified chain: global webview drop (dragdrop.ts:38) -> main.ts:98 compressPaths -> shouldPickPreset true for default quick-pick layout (list.ts:59,564,767) -> openQuickPickForPaths does el.appendChild(overlay) into the hidden view-videos el (list.ts:730,100; setTab main.ts:75), which [hidden]{display:none!important} (styles.css:83) fully hides; quickPick is set (list.ts:731) so onKeyDown swallows ALL keys at the return on line 404, which precedes the `if(el.hidden)return` guard at line 406. No toast, no tab-switch, no el.hidden guard in the drop path. Only nit: Escape (list.ts:377) does recover it, so it's an invisible freeze until the user blindly hits Esc rather than literally permanent, but the no-feedback core-flow break holds.

### [S3] Converted tab shows nothing while loading and resets selection to top on every refresh

- **Evidence:** createConvertedView.refresh (converted.ts:351-373) awaits listConversions() with no loading indicator â€” the scroll keeps the previous content (or is blank on first open) until the promise resolves. On every refresh it does `selEl=null; setSelected(navRows()[0])` (lines 371-372), and refresh runs on every tab switch (main.ts:87) and after every finished conversion while the tab is open (main.ts:110-112).
- **Impact:** On a slow disk / large journal the tab looks empty or stale with no spinner. And if a user has scrolled down and selected row 20, then a background conversion finishes (or they tab away and back), the selection silently jumps back to the first row and scrollIntoView yanks them to the top â€” losing their place mid-review.
- **Proposed fix:** Add a loading state (skeleton/spinner) for the first load, and preserve the current selection across refresh by matching on outputPath instead of always resetting to row 0; only default-select the first row when there was no prior selection.
- **Decision needed:** Should a finished-conversion refresh preserve scroll/selection, or is jumping to the newest (top) item the intended behavior when something just completed?
- **Verifier:** converted.ts refresh() does `selEl=null; setSelected(navRows()[0]??null)` (371-372), and setSelected calls node.scrollIntoView({block:"nearest"}) (310) on rebuilt DOM; refresh fires on every tab switch (main.ts:87) and on each `done` encode while tab open (main.ts:110-112), so a scrolled-down selection silently jumps to row 0/top. No loading indicator in refresh, though prior content persists during the await (only blank on first open), making that sub-issue minor. S3 fits.

### [S3] A failed conversion can never be retried from its row â€” clicking only dismisses it

- **Evidence:** onRowClick (list.ts:735-760): for a job in phase 'failed', it calls `dismiss(j); return` (lines 746-749) â€” the click is consumed by clearing the error and never reaches the enqueue/picker path below. The expanded chips can re-run, but the row itself, the most obvious target after seeing the red error, just makes the error vanish with no new conversion and no explanation.
- **Impact:** After an encode fails, the natural reaction is to click the row to try again. Instead the error silently disappears and nothing happens â€” the user has to discover that they must expand (e) and pick a chip, or re-drop. Feels like the retry click was swallowed.
- **Proposed fix:** On a failed row, either fall through to re-open the picker / re-run the same preset (a true retry), or show an explicit 'Retry' affordance. Dismiss-only should not be the response to the primary click target.
- **Decision needed:** Should clicking a failed row retry with the same preset, open the picker fresh, or keep dismiss-and-stop â€” and should there be a visible Retry button regardless?
- **Verifier:** list.ts:746-749 â€” for a failed job onRowClick calls `dismiss(j); return`, which (dismiss, lines 174-183) only clears the error/state and re-renders, never falling through to the enqueue/quick-pick path at 752-759. Non-failed jobs (line 750) dismiss then proceed to re-enqueue, so the failed row is uniquely a no-op retry that just erases the red error. Real UX inconsistency; S3 is correct (no data loss, re-drop still works).

### [S3] Tablist has incomplete ARIA: no aria-selected, no keyboard tab navigation

- **Evidence:** main.ts:28-32 declares role=tablist with three role=tab buttons, but setTab (main.ts:74-91) only toggles the `is-active` CSS class â€” it never sets aria-selected, and the markup has no aria-controls or roving tabindex. There is no arrow-key handler to move between tabs (the seg-btns are reachable only by raw Tab key and activated by click; the global Arrow handlers in list/converted are for the list, not the tablist).
- **Impact:** Screen-reader users hear three generic tabs with no indication of which is selected, and the standard tab-pattern keyboard interaction (Left/Right to move between tabs) doesn't work. For an app that otherwise leans heavily on keyboard hotkeys, the primary navigation control is the least keyboard-friendly element.
- **Proposed fix:** Set aria-selected on each seg-btn in setTab, add aria-controls pointing at each view, give the views role=tabpanel, and implement roving tabindex + ArrowLeft/ArrowRight on the tablist.
- **Decision needed:** Is full WAI-ARIA tab-pattern compliance in scope for this release, or is correct aria-selected the minimum bar?
- **Verifier:** main.ts:28-32 sets role=tablist/role=tab; setTab (74-91) only toggles is-active class â€” no aria-selected/aria-controls anywhere, no roving tabindex, click-only activation (93-95). Arrow handlers are view-scoped: converted.ts:333/337 guarded by el.hidden on list rows, list.ts:428/433 guarded by active-bar mode cycling presets â€” neither moves tabs. Buttons remain native (Tab+Enter works), but the ARIA tabs pattern and selected-state announcement are absent. S3 correct.

### [S3] Toast and activity drawer occupy the same bottom slot and overlap

- **Evidence:** The toast is position:absolute; left:12px; right:12px; bottom:12px; z-index:50 (styles.css:1555-1560). The drawer is position:absolute; left:8px; right:8px; bottom:8px; z-index:50 (styles.css:1416-1421). Both anchor to the panel bottom with equal z-index and full width. There is no coordination: showToast (toast.ts:9) just unhides its element, and the drawer renders whenever any job is running/just-finished (drawer.ts:120-138).
- **Impact:** Whenever a conversion is running (drawer visible) and any toast fires â€” 'Copied to clipboard' from the drawer's own copy button, 'No video files in that drop', or any error string â€” the toast lands directly on top of the drawer at the same bottom edge, hiding the drawer rows/close button or being half-occluded by it. Looks broken during the exact moment (active conversions) the app is busiest.
- **Proposed fix:** Stack them: when the drawer is visible, offset the toast above it (e.g. bottom = drawer height + gap), or render the toast inside the drawer's z-context above it, or move the drawer to a different corner. A shared bottom-anchored container that stacks children would be cleanest.
- **Decision needed:** Should transient toasts sit above the persistent drawer, or should the drawer yield (slide up) while a toast is showing?
- **Verifier:** Both .toast (bottom:12px, z-index:50, styles.css:1555-1560) and .drawer (bottom:8px, z-index:50, styles.css:1416-1421) are full-width position:absolute siblings inside position:relative .panel (styles.css:127; toast at main.ts:37, drawer appended at drawer.ts:33). showToast (toast.ts:9) just unhides with no coordination, and the drawer's own Copy button fires showToast("Copied to clipboard") (drawer.ts:104) while the drawer is visible â€” guaranteed overlap. Nuance: with equal z-index the later-inserted drawer paints OVER the toast (occluding it), not the reverse as the finder states; the collision is still real. Cosmetic/UX, not broken functionality â€” S3.

### [S3] Play and Reveal actions give no success feedback; failures are the only signal

- **Evidence:** In converted.ts, copy shows 'Copied to clipboard' (line 69,116) but playButton/openFile (lines 67,103-107) and revealButton/reveal (lines 70,121-128) only `.catch` to toast on error â€” success is silent. Same in list.ts: reveal (line 990) and the row's openFile paths only toast on failure. On Windows especially, reveal/open can take a beat to bring Explorer/the player forward.
- **Impact:** User presses Play or Reveal (or hits `r`/Enter), the panel often hides on blur as focus leaves, and if the external app is slow or backgrounded they get zero confirmation the click registered â€” so they click again, spawning duplicate Explorer windows / player instances. Inconsistent with copy, which always confirms.
- **Proposed fix:** Give a lightweight confirmation for reveal/play (brief toast like 'Revealed in Finder/Explorer' or a button press state), or at least a pressed/active visual on the button so the action is acknowledged even when the panel hides.
- **Decision needed:** Is a toast on every reveal/play too noisy, given the panel usually hides right after â€” would a button micro-animation be enough?
- **Verifier:** Verified: converted.ts copyButton toasts "Copied to clipboard" on success (line 115) but playButton (line 105) and revealButton (line 125) â€” plus keyboard helpers doPlay/doReveal (lines 67,70) and list.ts buildRevealButton (line 990) â€” only .catch to toast, so success is silent. Impact mechanism is real: a release-only hide-on-blur handler (lib.rs:251-272) hides the panel on Focused(false) unless dialog/pinned/mouse-down; launching Explorer/player via openFile/reveal trips none of those guards, so the panel vanishes with no confirmation, inviting a repeat click. Note reveal is even worse than stated: the backend reveal command (commands.rs:351-356) is fire-and-forget and only logs errors server-side, so reveal gives no feedback on success OR failure. S3 (consistency/polish) is correct.

### [S4] Drawer can grow unbounded with many concurrent jobs and has no count cap or empty-after-cancel messaging

- **Evidence:** drawer render() appends one row per job with `.drawer-rows{max-height:220px;overflow-y:auto}` (styles.css:1463-1464) but no cap on job count â€” every queued/running/done job in the last 8s is listed (drawer.ts:121-137). Dropping N files enqueues N jobs at once (list.ts:663,771), all 'queued', all shown. Cancelled jobs are removed immediately (drawer.ts:142-152), so cancelling the last in-flight job makes the whole drawer vanish instantly with no 'cancelled' acknowledgement.
- **Impact:** Drop 30 files and the drawer becomes a 220px scroll of 30 'queued' rows covering the list; and cancelling your only running job makes the activity panel disappear mid-glance with no confirmation it was the cancel (vs. a crash). Acceptable but rough at the edges for power users batching drops.
- **Proposed fix:** Collapse queued jobs into a single 'N queued' summary row (expand on demand), cap visible rows with a '+N more' line, and consider a brief 'Cancelled' flash before the drawer clears rather than an instant disappearance.
- **Decision needed:** What's the expected max batch size, and should queued items be summarized rather than listed one-per-row?
- **Verifier:** All three sub-claims hold against real code. drawer render() (drawer.ts:120-137) appends one row per jobs.values() with no count cap into .drawer-rows{max-height:220px;overflow-y:auto} (styles.css:1463-64); every job-state event is fed in unconditionally via main.ts:108. Drops enqueue N jobs at once (list.ts:663 applyQuickPick, list.ts:771 compressPaths both `for(path of paths) doEnqueuePath`), and the backend worker is strictly serial (encoder/mod.rs:142-147 single `while let rx.recv()` loop), so N drops yield 1 running + (N-1) "queued" rows simultaneously â€” if anything worse than stated. On cancel, drawer.ts:142-152 deletes the job and re-renders with no 'cancelled' row/toast; if it was the only job, render() hides the whole drawer (drawer.ts:122-126). No guard refutes any part. S4 is correct: cosmetic/UX roughness, not broken functionality, data loss, crash, or a11y blocker.

### [S4] Quick-pick / Custom hotkeys are undiscoverable from the picker for keys 10+ and the Custom shortcut

- **Evidence:** openQuickPickForPaths renders the number badge only for the first 9 presets (`i < 9 ? i+1 : ''`, list.ts:694) and the hint reads '1â€“9 apply' (line 726), but onKeyDown only maps keys '1'â€“'9' (list.ts:397-403) â€” presets 10+ have no key and no badge, silently unreachable by keyboard. The Custom entry shows a 'C' badge (line 715) and `c` works (lines 389-396), but the hint line (line 726) never mentions C, so the keyboard shortcut for Custom is invisible.
- **Impact:** Users with more than 9 presets can't keyboard-select the extras and aren't told why; and nobody learns that `c` jumps to Custom because the picker's own hint omits it. Minor, but it's exactly the kind of inconsistency between what's shown, what's hinted, and what's wired that reads as unpolished.
- **Proposed fix:** Either cap presets at what the keyboard supports, or extend selection (0 for the 10th, then unreachable is honest), and add 'C custom' to the quick-pick hint when the Custom row is present.
- **Decision needed:** Is >9 presets a supported configuration, and if so how should keys 10+ be selected by keyboard?
- **Verifier:** All claims verified: list.ts:694 `${i < 9 ? i + 1 : ""}` blanks badges for preset index 9+; onKeyDown only maps "1"â€“"9" (lines 397-403, n=Number(e.key)-1) so presets 10+ have no key; presets are uncapped (preferences.ts:321 `d.presets.push`, orderedPresets returns all, no slice); Custom shows "C" badge (715) and c/C works (389-396) but hint at 726 ("1â€“9 apply Â· âŽ default Â· â†‘â†“ move Â· esc cancel") omits C. Extras remain mouse-clickable (clickâ†’applyQuickPick(i), 697), so keyboard-only undiscoverable, not fully unreachable. S4 is right.

## Accessibility

### [S2] Tabs declare role=tab but omit aria-selected, tabpanel, and arrow-key roving

- **Evidence:** src/main.ts:28-32 renders role="tablist" with three role="tab" buttons but never sets aria-selected; setTab() (main.ts:74-91) only toggles the .is-active class. The three views (listView.el, convertedView.el, prefsView.el) are plain divs toggled via .hidden with no role="tabpanel" / aria-labelledby. There is no keydown handler on the seg buttons (only click, main.ts:94), so Left/Right arrow does not move between tabs as the ARIA tab pattern requires.
- **Impact:** A screen reader announces 'tab' for all three but never says which is selected, and the content region isn't associated with its tab. Declaring the role without the required states/keyboard behavior is worse than no role â€” it actively misleads AT users. WCAG 4.1.2 / ARIA Authoring Practices violation.
- **Proposed fix:** Set aria-selected on the active seg-btn in setTab(), add role="tabpanel" + aria-labelledby to each view div, and either implement Left/Right arrow roving with roving tabindex, or drop the ARIA tab roles and let them be plain buttons (honest and still accessible).
- **Decision needed:** Do we commit to the full ARIA tabs pattern (roving tabindex + arrow keys), or simplify to labeled buttons given the app's heavy custom-key model?
- **Verifier:** Verified: main.ts:28-31 declares role="tablist"/role="tab" but no aria-selected is ever set (setTab main.ts:74-80 only toggles .is-active); view roots are plain divs `view view-converted`/`view view-videos` (converted.ts:50, list.ts:101) with no role="tabpanel"/aria-labelledby; seg buttons get only a click handler (main.ts:93-95) and the only keydown listeners are document-level list/converted nav, so no Left/Right roving. Genuine WCAG 4.1.2 / ARIA APG violation.

### [S2] Toast is not a live region â€” every error and 'Copied' message is silent to screen readers

- **Evidence:** The toast element is <div class="toast" id="toast" hidden> (src/main.ts:37) with no role="alert" or aria-live. showToast (src/lib/toast.ts:9-17) just sets textContent and flips hidden. Toast is the app's only channel for nearly all feedback: validation errors ('Target size must be greater than 0 MB', custom.ts:113; 'Preset name is required', preferences.ts:287), IPC failures (saveSettings/persist catch, preferences.ts:86), and success ('Copied to clipboard', converted.ts:69). Because it starts hidden and content is injected, even aria-live wouldn't fire without the right setup.
- **Impact:** A blind user who submits an invalid preset or whose conversion fails gets zero feedback â€” the form simply doesn't proceed with no announced reason. WCAG 4.1.3 (Status Messages) failure across the entire app's feedback layer.
- **Proposed fix:** Add role="status" aria-live="polite" (or role="alert" for errors) to the toast container, and keep the node present (not display:none) so injected text is announced â€” toggle a visual-only class instead of the hidden attribute, or re-set textContent after un-hiding.
- **Decision needed:** Should errors use assertive (role=alert, interrupts) while 'Copied'/success use polite, i.e. do we want two live regions with different urgencies?
- **Verifier:** Verified: toast div (main.ts:37) has no role/aria-live; showToast (toast.ts:9-17) only sets textContent + hidden; grep for aria-live/role=alert/status across src returns zero matches. Toast is the sole feedback for errors (custom.ts:113, preferences.ts:287/292), IPC failures and success (converted.ts:69) â€” all silent to screen readers. WCAG 4.1.3 failure. S2 stands.

### [S2] No visible focus indicator on any control â€” keyboard users can't see where they are

- **Evidence:** src/styles.css has exactly one focus rule, .input:focus (line 882), and one outline:none (line 873). There is NO :focus / :focus-visible style on .seg-btn, .btn-primary, .btn-ghost, .chip, .radio, .pin-btn, .conv-act, .folder-remove, .modal-back, .quickpick-item, .option-radio, or the toggle switches. Worse, the toggle switch's native checkbox is fully hidden (.switch input { opacity:0; width:0; height:0 } lines 1046-1051), and the visible .track has no input:focus + .track rule â€” so Tabbing onto any Behavior/Strip-audio toggle shows literally nothing. Default UA outlines won't appear on these because most are custom-painted backgrounds and the buttons rely on box-shadow/border, not outline.
- **Impact:** A keyboard-only or low-vision user pressing Tab through Preferences or the custom-convert form has no idea which control is focused. This is a hard WCAG 2.4.7 (Focus Visible) failure and the single most obvious 'amateur' accessibility tell a reviewer would flag in seconds.
- **Proposed fix:** Add a global :focus-visible rule (e.g. outline: 2px solid var(--accent); outline-offset: 2px;) on all interactive elements, and a dedicated .switch input:focus-visible + .track { box-shadow: 0 0 0 2px var(--accent) } so the toggle track shows focus. Keep :focus-visible (not :focus) so mouse clicks don't ring.
- **Decision needed:** Do we want a single uniform accent focus ring app-wide, or per-component rings tuned to each control's shape (pill tabs vs. square buttons)?
- **Verifier:** styles.css has only `.input:focus` (L882) and `outline:none` (L873); no `:focus`/`:focus-visible` on any custom control. The toggle switch is a hidden native checkbox (`.switch input{opacity:0;width:0;height:0}` L1046-1051) used in preferences.ts:344 and forms.ts:61, with no `.switch input:focus + .track` rule â€” so tabbing onto a Behavior/Strip-audio toggle shows nothing (WCAG 2.4.7 fail). Minor overstatement: native radios (.option-radio, .radio-item input) and non-outline-suppressed .seg-btn still get UA focus rings, so it's not literally "any control" â€” but the toggle and custom buttons (box-shadow/border-painted) are real failures. S2 stands.

### [S3] Placeholder text fails contrast in both themes

- **Evidence:** Computed WCAG ratios: dark --placeholder #5b5b63 on --surface-2 #222226 = 2.36:1; light --placeholder #a0a0aa on --surface-2 #e9e9ec = 2.14:1 (styles.css :root lines 46/73, input bg line 869, ::placeholder line 879). Both are far below the 4.5:1 needed for normal text. The filter input ('Filter recordingsâ€¦', list.ts:108), preset name ('e.g. Slack (25MB)'), and the auto/10/30 number-field placeholders all use this. In several number fields the placeholder is the only hint that 'auto' is the empty meaning (custom.ts:74-76).
- **Impact:** Low-vision users can't read the placeholder hints, and where the placeholder communicates the default behavior ('auto'), the meaning is lost. WCAG 1.4.3 (Contrast). The number-field 'auto' case is functional, not just cosmetic.
- **Proposed fix:** Darken --placeholder in light and lighten it in dark to reach >=4.5:1 (e.g. dark ~#787883, light ~#73737d), or render persistent helper text instead of relying on low-contrast placeholders for meaning like 'auto'.
- **Decision needed:** Do we accept slightly louder placeholders (they compete visually with real input), or move the 'auto'/example hints into always-visible field-hint text and keep placeholders purely decorative?
- **Verifier:** Verified: --placeholder #5b5b63 (styles.css:46) on --surface-2 #222226 (line 31) = 2.36:1; light #a0a0aa (line 73) on #e9e9ec (line 58) = 2.14:1, both <4.5:1 WCAG 1.4.3. .input bg=var(--surface-2) (869), ::placeholder=var(--placeholder) (879). Used by filter input (list.ts:108) and "auto" number fields via numberInput(null,"auto") (custom.ts:74-76, forms.ts:28); field-hint (custom.ts:104) doesn't convey the "auto" default, so the placeholder is the sole signal. S3 correct.

### [S3] Form inputs and switches have no programmatic labels â€” only adjacent text spans

- **Evidence:** field() (src/lib/forms.ts:11-19) wraps an input next to a <span class="field-label"> with no <label for>, no id, no aria-labelledby â€” so Target MB / Max FPS / Max width / Scale % / shortcut fields / recents-limit have no accessible name binding. The toggle/switch rows are <label> wrappers (toggleRow preferences.ts:340, switchRow forms.ts:57) which is fine, but the radio groups built in radioRow (forms.ts:122) and the videos-layout option-rows have no group-level <fieldset>/legend or role=radiogroup with an accessible name, so 'Off/Smart/Static' and 'By parts/By duration' read as ungrouped radios with no idea what they configure.
- **Impact:** A screen reader announces 'edit, blank' for every numeric/text field in the preset editor and custom page, and reads split-mode radios with no context. Users can't tell which field is which. WCAG 1.3.1 (Info & Relationships) / 4.1.2.
- **Proposed fix:** In field(), use a real <label htmlFor> with a generated id (or wrap input inside the label). Group each radio set in a <fieldset><legend> or role="radiogroup" aria-label so its purpose is announced. The split control's 'Parts (2â€“20)'/'â‰ˆ every N seconds' labels especially need binding.
- **Decision needed:** Is wrapping inputs inside <label> acceptable styling-wise (changes the .field flex layout), or do we prefer explicit for/id pairs to keep the current markup?
- **Verifier:** field() (forms.ts:11-19) emits a <span class="field-label"> + input with no <label for>/id/aria-labelledby; grep over src shows zero aria/for/htmlFor on any preset-editor (preferences.ts:258-274,447,604), custom (custom.ts:92-99), shortcut (preferences.ts:560) or split (forms.ts:175-176) input â€” these announce "edit, blank". Radios in radioRow (forms.ts:122, preferences.ts:354) and videos-layout (preferences.ts:404-430) ARE each <label>-wrapped so they get per-option names, but the groups lack a fieldset/role=radiogroup name. WCAG 1.3.1/4.1.2; S3 correct.

### [S3] No prefers-reduced-motion support â€” infinite shimmer and all transitions ignore the OS setting

- **Evidence:** styles.css has no @media (prefers-reduced-motion) block anywhere. The preview-loading shimmer animates infinitely (animation: preview-shimmer 1.6s linear infinite, line 586; keyframes 594-601) whenever a preview is being prepared, and the app is dense with transitions (rows, chevrons rotate 90deg, switch knobs slide, bars). theme.ts already uses matchMedia for color-scheme but motion is never queried.
- **Impact:** Users with vestibular sensitivity who set 'Reduce motion' at the OS level still get the continuously animating shimmer and sliding/rotating affordances. WCAG 2.3.3 (Animation from Interactions, AAA) and a common professional-polish expectation; for a menu-bar utility the perpetual shimmer is the most noticeable.
- **Proposed fix:** Add @media (prefers-reduced-motion: reduce) that sets animation: none on .preview-loading and reduces/zeroes transition durations globally; replace the shimmer with a static 'Preparing previewâ€¦' state.
- **Decision needed:** Reduce-motion should kill decorative animation â€” but do we keep the progress-bar width transition (it conveys real state) or also flatten it to instant jumps?
- **Verifier:** No `prefers-reduced-motion` anywhere in src (grep: 0 matches); infinite shimmer at styles.css:586 (`animation: preview-shimmer 1.6s linear infinite`, keyframes 594-601), 23 transition/animation rules in styles.css, while theme.ts:4 queries only `prefers-color-scheme`. OS Reduce-motion is honored for color but ignored for motion. S3 correct (AAA accessibility polish, not functional breakage).

### [S3~] Icon-only row controls are keyboard-unreachable and unlabeled for screen readers

- **Evidence:** buildRevealButton (src/views/list.ts:978-993) sets btn.tabIndex = -1 (line 983) with only a title (revealLabel()) and no aria-label; the SVG is aria-hidden. The active-bar arrows are also tabIndex=-1 (list.ts:618, 629). So the per-row 'reveal in file manager' magnifier and the prev/next preset arrows cannot be reached by Tab at all, and a screen reader reading the reveal button announces just 'button' (title is not a reliable accessible name for SR). The row itself is a <div> with a click handler and no role/tabindex, so its primary action is reachable only via the arrow-key scheme, not standard Tab.
- **Impact:** Revealing a file in Finder/Explorer and switching the active preset via the bar are impossible without a mouse for users who navigate by Tab, and are mislabeled/silent under VoiceOver/NVDA. WCAG 2.1.1 (Keyboard) and 4.1.2 (Name/Role/Value) failures.
- **Proposed fix:** Give icon-only buttons aria-label (reveal, copy in drawer already does this, reveal in list does not). Decide whether reveal/arrows should join the Tab order (remove tabIndex=-1) or be exposed via documented keys; if the latter, still add an aria-label and ensure the row is operable. At minimum add aria-label to buildRevealButton.
- **Decision needed:** Should the magnifier and active-bar arrows be in the natural Tab order, or is the intent that the whole app is driven by the custom arrow-key model â€” and if so, how does a screen-reader user discover those keys?
- **Verifier:** Reveal half CONFIRMED: buildRevealButton (list.ts:983) sets tabIndex=-1 with only title=revealLabel() and aria-hidden SVG, no aria-label; reveal() is only called from the click handler (line 990) and is absent from onKeyDown's switch, so it has zero keyboard path and announces just "button" to SR. The row is a bare <div> (996) with no role/tabindex. BUT the finding's active-bar claim is REFUTED: although prev/next arrows are tabIndex=-1 (618/629), preset switching is fully keyboard-operable via [ / ] / ArrowLeft / ArrowRight at list.ts:428-437 (titles even say "Previous preset ([)"), so "switching the active preset is impossible without a mouse" is false. Because the headline impact overstates, downgrading.

### [S3] Quick-pick overlay and custom-convert page are not announced as dialogs and don't trap focus

- **Evidence:** openQuickPickForPaths (src/views/list.ts:669-733) builds .quickpick-overlay with no role="dialog"/aria-modal/aria-label and never moves DOM focus into it â€” it relies on the document-level keydown handler (list.ts:374-405) to intercept keys. The custom page (src/views/custom.ts) calls targetInput.focus() (line 152) but sets no role="dialog"/aria-modal and nothing prevents Tab from moving focus to the still-present list/tabs behind it. The scrim is pointer-events:none-free but the underlying panel stays in the tab order.
- **Impact:** Screen-reader users aren't told a modal opened or what it's for; sighted keyboard users Tabbing in the custom form can walk focus out behind the page onto the tabs/rows that are visually covered, then operate hidden controls. WCAG 2.4.3 (Focus Order) / 4.1.2.
- **Proposed fix:** Add role="dialog" aria-modal="true" and an aria-label/labelledby to both overlays, move focus to the first control on open, restore focus to the trigger on close (custom already re-focuses the filter via focusFilter, good), and implement a Tab focus trap or inert the background.
- **Decision needed:** Is the quick-pick meant to be a true modal dialog (trap + SR announcement) or an ephemeral keyboard menu â€” and should we use the <dialog> element to get trapping/inert for free?
- **Verifier:** Verified: neither .quickpick-overlay (list.ts:675-732) nor .modal-page (custom.ts:34-35,150-152) sets role="dialog"/aria-modal/aria-label (grep finds aria only in converted.ts/drawer.ts/an SVG). Quick-pick never moves DOM focus in, relying on the document keydown handler (list.ts:374-405); custom page focuses targetInput (custom.ts:152) but has no focus trap and nothing sets inert/aria-hidden on the background. modal-page is position:absolute;inset:0;z-index:40 over .panel (styles.css:911-916) and is mounted into .panel (list.ts:791), which still contains the natively-focusable role="tab" seg-btns and pin-btn (index.html) plus the list's filterInput/rows â€” all reachable by Tab behind the opaque overlay. Real WCAG 2.4.3/4.1.2 Level A failure; S3 is correctly rated.

## Robustness & error handling

### [S2] Cancel during the Verifying phase still trashes the original and copies to clipboard; job ends Done, not Cancelled

- **Evidence:** src-tauri/src/encoder/mod.rs:529-566 (run_single) and the mirror at 645-670 (run_split_set): once run_video_convergence/run_gif/encode_part_set return Ok, the code sets Phase::Verifying, calls enforce_target, then run_post_actions(...) â€” which moves the original to Trash (line 1212-1219) and writes the clipboard â€” and finally Phase::Done, with NO is_cancelled() recheck anywhere in this window. The is_cancelled closure is only consulted inside the ffmpeg engines (e.g. 481-484, 1736). A cancel() that arrives after the last ffmpeg pass exits but before run_post_actions only lands in the cancelled HashSet; run_single returns Ok, so process_job's was_cancelled cleanup branch (308-344) never runs.
- **Impact:** User clicks Cancel on a job that is showing 'Verifying' (or between split parts) and the app still deletes their source recording to the Trash and lands the job as Done. With trash_original on, that is silent data movement the user explicitly tried to stop â€” the worst-feeling class of bug for a 'cancel' button.
- **Proposed fix:** After the engine returns Ok and before run_post_actions, re-check is_cancelled(); if set, treat it as a cancel: remove the just-promoted output (+ part siblings / part folder for splits), skip post-actions, and flip the job to Cancelled. The trash step in particular must never run once a cancel has been observed.
- **Decision needed:** Should a cancel that arrives after the encode has fully finished but before delivery be honored as a cancel (discard the finished output), or is it 'too late' and we deliver anyway? Trashing the original on a cancelled job is clearly wrong either way â€” confirm the intended cancel semantics for the post-encode window.
- **Verifier:** Real race, no guard. run_single (mod.rs:527-566) and run_split_set (645-670) take the engine's Ok and go straight Verifying->enforce_target->run_post_actions (trashes original at 1212-1219, writes clipboard at 1190-1210)->Done with no is_cancelled() recheck. The only cancel hooks are inside the ffmpeg engine (1736/1753); after the last pass exits, child_slot is take()n (1785) so a late cancel()'s start_kill finds None (227-234) and just sits in the HashSet. run_job returns Ok, so process_job's Err/was_cancelled cleanup (307-344) never runs â€” job ends Done. A cancel landing in the Verifying window (or between split parts) still trashes the source. Real but a narrow timing window, and source goes to Trash (recoverable), so S2 not S1.

### [S3] ffprobe returning width/height/fps of 0 is accepted and silently disables the quality-floor planner instead of failing

- **Evidence:** probe.rs:57-63 builds ProbeInfo with width/height = as_u64().unwrap_or(0) and fps = parse_frame_rate(...) which yields 0.0 on garbage ('0/0' â†’ 0.0, line 124). Only the video stream's presence and a positive duration are required. plan.rs:151 then gates ALL auto-degradation on 'eff_w>0 && eff_h>0 && fps_eff>0', so a probe that returns zero geometry/fps leaves bpp = INFINITY (plan.rs:150) and the planner applies NO fps/width auto-cap; the worker's hardware-viability gate (mod.rs:95-97) also never triggers.
- **Impact:** A weird/corrupt container that ffprobe parses but reports zero geometry for (some MediaRecorder WebMs, broken MKVs) skips the starvation safeguards entirely. The encode then runs the raw planned bitrate at full resolution and is far more likely to overshoot, burn all retries, and fail with 'Couldn't fit under N MB' â€” when downscaling would have worked â€” or produce a poor-quality result. No diagnostic tells the user the probe was incomplete.
- **Proposed fix:** Treat zero width/height (and zero fps for non-GIF bitrate plans) as a probe failure or at least log a prominent warning; consider falling back to the packet/stream scan for geometry, or refusing the job with a clear 'couldn't read this video's dimensions' rather than proceeding blind.
- **Decision needed:** How common are zero-geometry probes in tamp's real input population (screen recorders)? If rare, is a hard error preferable to a degraded-quality best-effort encode?
- **Verifier:** Verified: probe.rs:59-61 accepts width/height=as_u64().unwrap_or(0) and fps=parse_frame_rate(...)=0.0 ("0/0"â†’0.0 line 109, ""â†’0.0 line 112), requiring only a video stream (line 45) and positive duration (lines 49-55). With zero geometry, plan.rs:137-145 yields eff_w/eff_h=0, so the auto-degradation block gated at plan.rs:151 (eff_w>0&&eff_h>0&&fps_eff>0) is skipped, leaving bpp=INFINITY (plan.rs:150); mod.rs:95-97 hardware_viable then never gates (INFINITY>=HW_MIN_BPP). No warning is logged for incomplete geometry. Note: plan.rs:148-150 and mod.rs:92-94 comments show "never trigger on a guess" is deliberate, and the convergence loop is a backstop, so it's degraded behavior on rare corrupt containers, not a hard crash â€” S3 is correct.

### [S3] Journal and settings .bak backups overwrite each other and the journal write is non-atomic (truncate-in-place)

- **Evidence:** journal.rs:131-139 backup_corrupt copies the corrupt file to a FIXED path '{file}.json.bak' every time a corrupt journal is loaded; settings.rs:302-317 backup_store_file does the same for settings. journal.rs:123 persists via std::fs::write(path, json), which truncates then writes in place â€” a crash, power loss, or ENOSPC mid-write leaves a truncated/corrupt conversions.json. The self-heal then copies THAT over any previously-saved .bak, destroying the earlier good backup.
- **Impact:** On a full disk or crash during a journal append, the journal becomes corrupt; on next launch it is 'backed up' (overwriting any prior real backup) and reset to empty, so the reuse-provenance gate and the Converted-tab history silently lose all prior records. Reuse may then re-encode or, worse, treat a mismatched output as reusable. Low frequency but real data loss of history.
- **Proposed fix:** Write the journal atomically: serialize to a temp sibling and rename onto conversions.json (rename is atomic same-dir, like the encoder's own .part promote). Make the corrupt-backup name unique (timestamped) or skip backing up over an existing .bak, so a self-heal can't clobber the last good copy.
- **Decision needed:** Is the conversion journal considered disposable cache (then corruption is merely annoying) or user-facing history worth protecting with atomic writes and retained backups?
- **Verifier:** Both claims verified. journal.rs:123 persists via std::fs::write (truncate-in-place, no temp+rename) so a crash/ENOSPC mid-write corrupts conversions.json. journal.rs:60-66 self-heals by calling backup_corrupt, which (journal.rs:131-133) copies to a FIXED path path.with_extension("json.bak") via std::fs::copy that unconditionally overwrites; settings.rs:310-311 is identical. The .bak is only ever written, never read back to restore (grep shows no consumer), so a single corruption already silently resets journal history; a later corruption overwrites the only surviving copy. Impact chain is real: reuse_is_stale (mod.rs:941-943) returns false when no record exists, so lost provenance makes a mismatched-but-under-target output reusable (size still gated by enforce_target, mod.rs:966). Best-effort secondary data (history/provenance), low frequency, no user-file loss â€” S3 is correct.

### [S3] context_menu register/unregister is not atomic across the 6 extensions; a mid-loop failure leaves a partial registration with the UI toggle reverted

- **Evidence:** src-tauri/src/platform/context_menu.rs:23-46: register() loops EXTS writing HKCU keys and unregister() loops delete_subkey_all, each returning Err on the first failing extension with no rollback of the ones already processed. commands.rs:397-405 set_context_menu calls apply(enabled)? FIRST and only persists the setting if apply succeeded; preferences.ts:526-533 reverts the toggle on the rejected promise. So if extension 3 of 6 fails (e.g. registry policy / locked key), extensions 1-2 are already written, 4-6 are not, the setting is NOT saved, and the UI shows the toggle off.
- **Impact:** The right-click 'Compress with tamp' entry appears for some video extensions but not others, while Preferences shows the feature as disabled. Re-toggling can't cleanly recover because register/unregister both abort on the same failing key. The user sees an inconsistent, half-working integration with no actionable message beyond the raw registry error.
- **Proposed fix:** Make the loop best-effort: collect per-extension errors instead of early-returning, attempt all six, then report a combined result. On any register failure, attempt unregister() of what was written so the on-disk state matches the (reverted) toggle. Surface a count ('registered 4 of 6 file types') rather than a raw OS error.
- **Decision needed:** Is a partial registration acceptable as 'works for most types', or must it be all-or-nothing? If all-or-nothing, do we roll back on partial failure or retry the failing keys?
- **Verifier:** context_menu.rs:25-31 register() and :38-43 unregister() loop the 6 EXTS with `?`/early-return and no rollback; commands.rs:399 calls apply()? before persisting (:403-404), so a mid-loop failure leaves partial registry keys, the setting unsaved, and preferences.ts:531-533 paint() reverts the toggle to off â€” exactly the inconsistent half-registered state described. Real, but the trigger (HKCU write failing partway â€” needs reg policy/locked key) is an uncommon edge, so S3 not S2.

### [S4] run_post_actions copies to clipboard BEFORE confirming the trash succeeded, but a failed clipboard write does not stop the (irreversible) trash

- **Evidence:** mod.rs:1183-1222: the function copies to clipboard, then unconditionally proceeds to trash::delete(input) regardless of whether the clipboard copy failed â€” both failures are merely concatenated into post_error. With copy_to_clipboard + trash_original both on, a clipboard failure (busy clipboard, see the Windows finding) still trashes the original. The user wanted 'compress, put on clipboard, remove original' as one atomic intent.
- **Impact:** On a clipboard hiccup the original is gone to Trash AND nothing made it to the clipboard â€” the user has to recover the file from Trash and manually copy it. The combined post_error chip explains it after the fact, but the ordering makes a recoverable clipboard glitch compound with an irreversible-feeling trash.
- **Proposed fix:** When both toggles are on and the clipboard copy fails, skip (or defer) the trash and surface 'kept the original â€” couldn't copy to clipboard' so the destructive step doesn't proceed on a half-failed post-action set.
- **Decision needed:** Should trash be conditional on clipboard success when both are enabled, or are these independent user preferences where trashing should proceed regardless of the clipboard outcome?
- **Verifier:** mod.rs:1190-1220: copy_to_clipboard failure only pushes to `failures` (line 1208) and falls through; trash::delete(input) at line 1213 runs unconditionally with no success-guard, so a clipboard glitch + both toggles on still irreversibly trashes the original while nothing reaches the clipboard. Both errors are joined at line 1222 exactly as described. Recoverable from system Trash with the post_error chip explaining it, hence S4 is fair.

### [S4] Hardware-encoder probe failure is cached for the whole process lifetime; a transient ffmpeg -encoders hiccup disables HW until restart

- **Evidence:** hw.rs:8 + 13-41: available_candidates() memoizes the result in a OnceCell. If the very first encoder_list() call fails (hw.rs:48-57: ffmpeg spawn error, non-zero exit, or a momentary AV/permission block on the bundled ffmpeg), it logs 'hardware encoding disabled' and caches Vec::new() permanently. Every MP4 encode for the rest of the session then goes straight to two-pass software with no re-probe.
- **Impact:** A one-off failure to run the bundled ffmpeg -encoders at the first conversion (antivirus scan of the freshly extracted binary, transient sandbox/permission state on Windows) silently forces all subsequent encodes onto slower software for the entire session. The user just experiences 'tamp is slow today' with no indication or way to recover short of relaunching.
- **Proposed fix:** Cache only successful probes; on an Err result, return an empty slice WITHOUT memoizing so a later encode re-probes. Optionally retry once. Keep the success path cached as today.
- **Decision needed:** Is re-probing on every failure (until one succeeds) acceptable overhead, or should there be a bounded number of attempts before giving up for the session?
- **Verifier:** hw.rs:8 OnceCell + hw.rs:14-24 caches Vec::new() on any encoder_list() failure (spawn err / non-zero exit, hw.rs:54/56); sole caller mod.rs:1081 only reads the memoized value, so an empty result forces every subsequent MP4 encode to two-pass software (mod.rs:1079-1091) with no re-probe for the session. S4 fits: rare trigger, output stays correct (just slower), no crash/data loss.

### [S4~] Windows clipboard copy fails the whole multi-part paste if the clipboard is briefly locked, surfaced only as a per-job warning chip

- **Evidence:** platform/windows.rs:64-68: Clipboard::new_attempts(10) tries to open the clipboard up to 10 times (clipboard_win's internal ~1ms spin) then errors 'cannot open clipboard'. macos.rs:150-154 has a 2s main-thread timeout. On failure run_post_actions (mod.rs:1197-1209) records a post_error string; list.ts:1161-1166 renders it as a small '.post-warn' chip on the Done row. There is no retry-from-UI affordance and the output is already delivered, so the user must manually locate and copy the file(s).
- **Impact:** Another app holding the clipboard (clipboard managers, RDP, password managers paste-on-copy) during the brief write window makes the auto-copy silently no-op for that conversion. The user pastes into Discord expecting the file and gets nothing/old contents, with only a subtle warning chip to explain it. The primary value prop (compress-and-paste) fails intermittently with no easy recovery.
- **Proposed fix:** Increase the open-attempt budget / add a short backoff retry around set_file_list on Windows (the open contention is transient), and give the post-error chip a 'Copy again' action that re-runs copy_files_to_clipboard for the delivered outputs.
- **Decision needed:** What's the acceptable retry budget/latency for grabbing a busy clipboard before giving up, and should a failed auto-copy offer an explicit retry button rather than only a passive warning?
- **Verifier:** Race is real: windows.rs:64 Clipboard::new_attempts(10) is a brief yielding retry vs macos.rs:150's 2s timeout, and a locked clipboard makes auto-copy no-op, recorded as post_error (encoder/mod.rs:1197-1209). But the finding's framing is overstated: it is NOT surfaced "only" as a chip â€” list.ts:194-198 noticePostError also raises a toast with the error; and recovery is NOT a manual file hunt â€” the Converted tab has a one-click Copy button (converted.ts:110-118), Copy-all (267), and a `c` hotkey (340). Requires another app holding the clipboard in a sub-ms window; clear recovery exists.

## Data integrity & persistence

### [S3] Journal corruption backup uses a fixed .bak path that overwrites any prior good backup, and persist is a non-atomic truncating write

- **Evidence:** journal.rs:131-138 `backup_corrupt` copies the bad file to a FIXED `path.with_extension("json.bak")`. settings.rs:302-317 does the same for settings.json.bak. If the journal is corrupt twice (e.g. a crash keeps truncating it), the second backup overwrites the first â€” the earliest, possibly-recoverable copy is lost. Separately, `persist` (journal.rs:108-126) does `std::fs::write(path, json)` directly over the live file: a crash or power loss mid-write leaves a truncated/half-written conversions.json, which then loads as corrupt on next start (and triggers the single-slot .bak overwrite). There is no temp-file + atomic-rename, even though the encoder itself is careful to use `.part` + rename for outputs (plan.rs:554-566).
- **Impact:** A power loss or crash during a journal append can corrupt the entire conversion history file, and the recovery backup can itself be clobbered by a repeat â€” so the "durable history" can be lost wholesale despite the backup mechanism that was meant to protect it.
- **Proposed fix:** Write the journal atomically (write to `conversions.json.tmp`, fsync, rename over the target) the same way outputs are handled, and make the corruption backup non-clobbering (timestamped name, or refuse to overwrite an existing .bak).
- **Decision needed:** What durability guarantee does the conversion history warrant â€” best-effort (current) or crash-safe atomic writes consistent with how output files are already protected?
- **Verifier:** All claims hold: journal.rs:123 persist uses std::fs::write (truncate+write, non-atomic, no .part/rename); journal.rs:132 and settings.rs:310 both back up to a FIXED path.with_extension("json.bak") via fs::copy, so a repeat corruption clobbers the prior backup; plan.rs:559-565 proves atomic .part+rename is used for outputs, confirming the inconsistency. Severity stays S3: the journal is explicitly best-effort/in-memory-tolerant (journal.rs:39) and only a 200-record annotation cache, not authoritative data, so "durable history lost wholesale" slightly overstates impact, but the corruption-window and single-slot-backup gaps are real.

### [S3] list_conversions re-stats every backfill-needed source from disk on every tab open, and never persists the result

- **Evidence:** commands.rs:378-392 â€” `list_conversions` clones all journal records and, for each `rec.input_created_ms == 0`, calls `file_created_ms()` which does a synchronous `std::fs::metadata(path)` on the original source path. The mutated `created_ms` is written only into the returned clone â€” it is never written back to the Journal or to disk (no `journal.append`/persist call). Every single open of the Converted tab therefore re-stats the original of every legacy (pre-`inputCreatedMs`) record again, even though the value can never change. The command is also a synchronous (non-async) Tauri command, so all those `metadata` syscalls run on the IPC thread; for sources on a slow/disconnected network drive this stat can block.
- **Impact:** Opening the Converted tab does N filesystem stats of original files (some on removable/network volumes that may hang) on every visit, repeated forever for old records â€” sluggish tab opens and a possible UI stall on disconnected volumes, all to recompute a value that is immutable once known.
- **Proposed fix:** Backfill once and persist: when a zero `input_created_ms` is resolved, write it back into the Journal (add a `backfill_created` method that updates + persists). Make `list_conversions` async / spawn_blocking so the stats don't run on the IPC thread. Records whose source is gone stay 0 and are simply not re-probed if marked tried.
- **Decision needed:** Should a resolved created-time be frozen into the journal (immutable provenance), or always reflect the source file's current creation time (which can change if the file is replaced)?
- **Verifier:** commands.rs:378-392: list_conversions is sync `pub fn` (no spawn_blocking like copy_file/pick_videos), clones all journal records, and for each input_created_ms==0 calls file_created_msâ†’std::fs::metadata (366-372) on the IPC thread; the backfilled ms is written only into the returned clone, never via journal.append/persist (journal.rs:83-91,108), so legacy records re-stat every tab open forever. S3 fits: bounded by MAX_RECORDS, only legacy records, stall only on slow/disconnected volumes.

### [S3] MAX_RECORDS=200 silently truncates the "durable" Converted history, and splits drop part-by-part

- **Evidence:** journal.rs:14,83-91 â€” `const MAX_RECORDS: usize = 200;` and `append()` does `records.drain(..excess)` on every append, keeping only the newest 200 ConversionRecords. converted.ts:45-47 advertises the tab as "a durable history of every conversion (journal-backed)". Each split PART is its own record (encoder/mod.rs:830 calls `append_journal` once per part inside the loop), so a single 10-part split job consumes 10 of the 200 slots. A user who does a few dozen split conversions silently loses the oldest singles. Worse, capping is per-record not per-job: convgroup.ts:37-58 groups parts by folder, so when the 200-cap cuts through the middle of a split set, `groupConversions` renders a group with a SUBSET of its parts (e.g. "3 parts" when 10 were made), and the `totalBytes`/`Copy all` only cover the surviving parts.
- **Impact:** The Converted tab is sold as a complete durable history but silently forgets old conversions, and a partially-evicted split shows a corrupt part count and an under-counted total size â€” a discerning user notices their history vanishing and split groups showing the wrong number of parts.
- **Proposed fix:** Either raise/remove the cap for the history file (200 records of ~200 bytes is ~40 KB â€” trivially small even at 10k records), or cap by job/group rather than by raw record so a split set is evicted atomically. If a cap stays, the empty-state and tab copy should stop promising "every conversion".
- **Decision needed:** Is the Converted tab meant to be a complete permanent history, or an intentionally bounded "recent conversions" list? That decision drives whether the cap is raised, made job-atomic, or kept with honest UI copy.
- **Verifier:** All cites verified: journal.rs:14 MAX_RECORDS=200, :86-89 per-record drain(..excess)+persist (irreversible); converted.ts:45 "durable history of every conversion"; encoder/mod.rs:731 part loop with append_journal at :830 once per part (10-part split = 10 slots); convgroup.ts:54/converted.ts:262,270 rebuild parts.length/totalBytes/Copy-all from surviving records only, so a split straddling the cap shows a corrupt part count and under-counted size. Real, but it's an undisclosed retention limit (journal.rs:13 keeps the file "small") and the mis-count is a transient one-job-at-a-time edge â€” S3, not S2.

### [S4] Legacy settings migration only re-seeds presets, not other now-required fields, and there is no schema-version marker

- **Evidence:** settings.rs:260-298 `load` deserializes via field-level serde defaults and then has exactly ONE explicit repair: `if settings.presets.is_empty()` re-seed presets + default_preset_id (lines 273-278). There is no `schema_version`/`version` field anywhere in `Settings`, so there is no way to know which migration shape a stored file is, and no migration hook for future field renames/removals â€” every future change must be expressible as a serde default or it silently drops user data. `default_preset_id` is only re-seeded when presets were empty; a store with non-empty presets but a `default_preset_id` that points at a now-deleted preset loads as-is and is only caught later by `validate` on the next save, not on load.
- **Impact:** Forward migrations are limited to "add a field with a default"; any structural change (rename/remove/split a field) will silently lose the old value with no version to detect it, and a dangling default_preset_id can load without repair. This is the kind of brittle migration story that bites on the first non-additive settings change.
- **Proposed fix:** Add a `schema_version` field and a small migration switch in `load` that runs versioned transforms before deserializing into the current `Settings`, and repair a dangling `default_preset_id` at load time (point it at the first preset).
- **Decision needed:** How far back must settings.json compatibility reach, and is an explicit schema-version + migration ladder worth adding now before the first non-additive field change ships?
- **Verifier:** settings.rs:153-197 has no schema_version field; load() (260-298) has exactly one repair â€” re-seed presets+default_preset_id only when settings.presets.is_empty() (273-278). A non-empty-presets store with a dangling default_preset_id skips that guard and loads unrepaired (load never calls validate; validate at 338-351 only catches it on a later save). Additive-only serde-default migration; non-additive changes silently drop data. Accurate; S4 correct (future-proofing + requires hand-edited/corrupt store).

### [S4~] Orphan annotation and reuse-staleness match output paths byte-for-byte â€” cross-entry-point case/separator differences break the join

- **Evidence:** journal.rs:98-106 `find_by_output` compares `r.output_path == output_path` with exact string equality. The journal stores `output.to_string_lossy()` derived from the job's input path (encoder/mod.rs:1289-1291); the scanner produces the lookup key from `entry.path().to_string_lossy()` rooted at the watched-folder string from settings (scanner.rs:115-118, used at commands.rs:36). On Windows these origins can differ in drive-letter case (`C:\` vs `c:\`) and separators when a job is started from the Explorer context menu / drag-drop (paths from a different API) vs a watched folder configured by the user with different casing. Any mismatch makes the orphan annotation silently fall back to `original_bytes:None, preset_name:None` (scanner.rs:108-112) and makes `reuse_is_stale` (mod.rs:944) treat the record as belonging to a different input â†’ re-encode instead of reuse.
- **Impact:** On Windows, orphaned outputs can show "â€”" for original size and a missing preset name even though a journal record exists, and a legitimate reuse can be skipped (wasted re-encode) â€” all because of cosmetic path-string differences the user never sees and can't diagnose.
- **Proposed fix:** Normalize paths to a canonical form (e.g. case-fold the drive letter, unify separators, optionally `dunce::canonicalize`) both when writing records and when looking them up, or store a normalized key alongside the display path and match on that.
- **Decision needed:** What is the canonical path identity on Windows for this app (case-insensitive? canonicalized? UNC-normalized?), given inputs arrive from watched-folder scans, the Explorer context menu, and drag-and-drop?
- **Verifier:** Half-right. The orphan-annotation fragility is real: journal.find_by_output uses exact `r.output_path == output_path` (journal.rs:104) and the orphan fallback yields original_bytes/preset_name None (scanner.rs:109-111); journal stores expected_output(&job.input,â€¦) rooted at input.parent() (plan.rs:511-512, mod.rs:1291) while the scanner key is rooted at the watched-folder setting string (commands.rs:25,36) â€” case/separator divergence breaks the join. BUT the reuse-re-encode claim is mechanically wrong: a case-mismatched output lookup makes find_by_output return None, so reuse_is_stale returns FALSE (mod.rs:941-942) â†’ reuse is KEPT, not skipped; line 944's input-string compare only runs after an exact output match, where the input casing matches too. Also, in the common default-folder path both Windows known-folder APIs and Explorer %1 return canonical-case C:\\, so divergence needs an atypically-cased user-configured folder â€” an uncommon edge, not the everyday case described.

### [S4~] Smart-split output names embed unvalidated preset.split.parts/seconds in the hash but only the active dimension is validated on save

- **Evidence:** settings.rs:78-91 `validate_split` only checks the dimension a STATIC split actually uses; for `SplitMode::Smart` (and `Off`) it returns Ok without bounding `parts`/`seconds`. preset_hash (encoder/plan.rs:447-460) only folds in the split dimension that is active, so this is consistent for the hash. But a hand-edited or migrated settings.json can carry e.g. `mode:"static", by:"parts", parts:0` for a non-default preset and pass through `from_value` (serde defaults at settings.rs:49-52 only fill MISSING fields, not zero ones). `plan_split` clamps `parts.clamp(1,20)` (plan.rs:276) so it won't divide by zero, but `validate` would reject `parts:0` ONLY if that preset's split is static-by-parts â€” a smart preset that later flips to static keeps the unvalidated 0 until the next save.
- **Impact:** A settings.json that has been hand-edited or partially migrated can persist out-of-range split values that survive load (validate is only enforced on save through the UI, not on load), so the persisted state is not guaranteed to satisfy the documented invariants.
- **Proposed fix:** Validate settings on LOAD (settings.rs:load) the same way save does, repairing or rejecting out-of-range values, so the in-memory Settings always satisfies its invariants regardless of how the file got its contents.
- **Decision needed:** Should load-time settings be trusted as-is (only UI-saved values are ever valid) or defensively validated/repaired on load against hand-edits and partial migrations?
- **Verifier:** Facts all check out: validate_split (settings.rs:78-91) only bounds the active STATIC dimension; load() (settings.rs:260-298) never calls validate (only save_settings does, commands.rs:64); serde defaults (settings.rs:49-52) fill only missing, not zero, fields. But every active consumer is hardened against the inert out-of-range value: plan_split clamps parts.clamp(1,20)/seconds.max(1) (plan.rs:276,300 â€” explicitly tested for 0 at plan.rs:1503,1531), preset_hash omits inert dimensions (plan.rs:447-460), and format.ts early-returns "smart split" before reading parts (format.ts:106). So the "invariant violation" requires a hand-edited JSON (out of threat model) and never produces an observable crash/wrong-output/wrong-hash. S4 floor is correct; this is defense-in-depth nit, not a defect.

### [S4~] Re-encoding an over-target output writes a SECOND journal record for the same output path, duplicating it in history

- **Evidence:** encoder/mod.rs:879 â€” when `check_reuse` finds the existing base output is over-target it `remove_file(expected)`s it but the journal record for that `output_path` is left in place. The fresh encode then targets the now-free `expected_output` base path (mod.rs:384-389 comment confirms this), and mod.rs:557 appends a NEW ConversionRecord with the SAME `output_path`. The journal now holds two records for one path. `find_by_output` (journal.rs:98-106) tolerates this by taking the newest, but converted.ts has no such dedup: groupConversions (convgroup.ts:44-46) pushes BOTH as separate `single` nodes, so the Converted tab shows the same output file twice (different sizes/times).
- **Impact:** After tamp's own over-target self-correction re-encode (or any re-run that retires a stale base output), the Converted history lists the identical output file twice â€” once with the old over-target size and once with the new size â€” which looks like a bug to anyone scanning their history.
- **Proposed fix:** Dedup on append: when appending a record whose `output_path` already exists, replace the prior record instead of pushing a duplicate (history of a single file path is a single latest fact). Alternatively dedup by `output_path` in `groupConversions`/`records()`.
- **Decision needed:** Is a per-output-path record meant to be "latest state of this file" (dedup/replace) or an append-only audit log of every encode attempt (keep both, but then the UI must show them distinctly)?
- **Verifier:** Mechanism is real: check_reuse (mod.rs:879) remove_file's an over-target base output without removing its journal record; the re-encode then targets the now-free base path (unique_output, plan.rs:626 sees it gone) and append_journal (mod.rs:557) pushes a SECOND ConversionRecord with the same output_path (journal.append, journal.rs:83-91, never dedups by path). list_conversions/groupConversions/singleRow (convgroup.ts:44-46, converted.ts:368-369) have no dedup, so the Converted tab shows the same file twice. BUT the trigger is a legacy/migration edge, not 'tamp's own self-correction': the current encoder never delivers OR journals an over-target output â€” enforce_target (mod.rs:966) deletes+fails before append_journal runs. The over-target path is documented (mod.rs:859) as cleaning up 'a stale artifact of the old keep-it policy', so a duplicate record requires a pre-existing legacy over-target file AND its old journal record AND a re-run under the new build.

### [S4] Orphaned-output journal lookup is O(records) per output and runs inside the recents loop on every scan

- **Evidence:** commands.rs:34-43 â€” `list_recents` loops every output video and calls `journal.find_by_output(&video.path)`, which (journal.rs:99-104) linearly scans the whole records Vec under a Mutex each time. With the 200-record cap this is small today, but it is O(outputs Ã— records) under the lock on every single recents refresh (every panel open / FS-change re-scan). If MAX_RECORDS is raised to make the history actually durable (finding above), this becomes O(NÂ²) on a hot path.
- **Impact:** Couples the recents-list refresh cost to history size; today negligible, but it silently caps how large the history can grow before the recents tab visibly slows, which constrains the fix for the durability problem.
- **Proposed fix:** Build a `HashMap<output_path, record>` index once (newest-wins) and pass it to the annotation loop, or expose a batch `find_by_outputs(&[String])` that locks once and indexes.
- **Decision needed:** Is the recents annotation expected to scale to thousands of historical records, or is the history intentionally small enough that linear scan is acceptable forever?
- **Verifier:** Accurate: list_recents (commands.rs:35-36) calls journal.find_by_output per is_output video, and find_by_output (journal.rs:99-105) does a linear .iter().rev().find under the Mutex; with MAX_RECORDS=200 (journal.rs:14) it's O(outputsÃ—records), negligible today as the finder admits. The O(NÂ²) concern is conditional on raising the cap, and the lock is taken per-call (not held across the loop), so it's a latent scalability/future-proofing note, not a current bug â€” S4 is correct.

## Platform & filesystem

### [S2] Windows tray icon contrast never tracks live light/dark theme changes â€” glyph can become invisible while idle

- **Evidence:** platform/windows.rs:24 tray_ink() reads SystemUsesLightTheme from the registry, but it is only consulted inside tray_progress() (windows.rs:134), which is called only on encode state transitions and the one startup idle paint (lib.rs:224). Grep confirms there is no WM_SETTINGCHANGE / theme listener anywhere (only the one registry read exists). So if the user flips Windows between Light and Dark mode while tamp is idle, the tray glyph keeps its old ink: white on a now-light taskbar (invisible) or near-black on a now-dark one (invisible) until the next encode happens to repaint it.
- **Impact:** The tray icon â€” tamp's only persistent UI surface â€” can vanish into the taskbar after a routine OS theme switch, making the app look crashed/uninstalled to the user until they start an encode.
- **Proposed fix:** Subscribe to Tauri's theme-changed / window theme event (or watch the Personalize registry key) and call tray_progress(app, None) to repaint the idle icon with fresh ink when the system theme changes.
- **Decision needed:** Is reacting to Tauri's app-level theme event sufficient on Windows, or do we need a dedicated WM_SETTINGCHANGE hook since the taskbar theme (SystemUsesLightTheme) is independent of the app theme tamp already listens for?
- **Verifier:** tray_ink()/taskbar_is_light() (windows.rs:12-32) is reached only inside tray_progress (windows.rs:154/173); its sole callers are the one startup idle paint (lib.rs:224) and encode lifecycle updates (encoder/mod.rs:287 via update_tray). tray.rs's on_tray_icon_event only caches the positioner rect + toggles the panel â€” it never repaints. Grep finds no WM_SETTINGCHANGE/WM_THEMECHANGED/ImmersiveColorSet listener anywhere. So an idle light<->dark OS theme flip leaves stale ink (white glyph on a now-light taskbar, or near-black on a now-dark one = invisible) until the next encode repaints. S2 holds: the tray is the only persistent surface and appears crashed.

### [S3] Unreachable watched folders (offline network drive, permission-denied) fail silently with no UI signal

- **Evidence:** scanner.rs:74-77 treats every read_dir error other than NotFound as a log_warn! + continue, returning a successful (possibly empty) Vec. list_recents (commands.rs:20) surfaces no per-folder status. A user who adds a UNC path like \\nas\recordings that is currently offline, or a folder they lack read permission on, gets an empty/short recents list and a 'No recent videos found' result from the compress-latest shortcut (shortcuts.rs:128) with zero indication that a folder is unreachable.
- **Impact:** Looks like tamp lost the user's recordings. With a flaky network share the recents list silently flickers between full and empty as the share connects/disconnects, and the user has no way to know the cause.
- **Proposed fix:** Return a per-folder reachability status from the scan and show an inline 'couldn't read <folder>' banner/row in the Videos tab (distinct from the legitimately-empty NotFound case), so the user can tell a missing share from no recordings.
- **Decision needed:** Do we want to actively warn about unreachable watched folders, or quietly tolerate them (current behavior) on the theory that transient network blips shouldn't nag?
- **Verifier:** scanner.rs:74-77 swallows every non-NotFound read_dir error (PermissionDenied/offline UNC) as log_warn!+continue, returning a successful Vec; list_recents (commands.rs:20) carries no per-folder status, and the UI shows misleading "No videos...record something!" (list.ts:489) / "No recent videos found" (shortcuts.rs:128) with zero signal the folder is unreachable. Real and reproducible; S3 is correct.

### [S3] 'Show in Explorer'/'Reveal in Finder' gives no feedback when the file was moved or deleted

- **Evidence:** commands::reveal (commands.rs:350-356) is declared `pub fn reveal(...) ` returning () and only logs on failure: reveal_item_in_dir errors are swallowed server-side. The frontend wires reveal(...).catch(e => showToast(...)) (converted.ts:70,125), but that catch can only fire on an IPC transport error, never on an actual reveal failure. So revealing an output that was deleted/moved/renamed externally (or lives on a now-disconnected drive) does nothing and shows no toast.
- **Impact:** Clicking 'Show in Explorer' on a stale Converted-tab entry appears to do nothing â€” no window, no error â€” which reads as a broken button. Inconsistent with copy_file/open_file, which DO return Result and surface errors.
- **Proposed fix:** Make reveal return Result<(), String> like open_file/copy_file so the existing .catch shows a 'File no longer exists at <path>' toast; optionally pre-check existence and offer to drop the stale row.
- **Decision needed:** Should a failed reveal just toast the error, or also prompt to remove the missing item from the Converted history?
- **Verifier:** commands.rs:350-356 `reveal` returns () and only `log_error!`s reveal_item_in_dir failures; ipc.ts:182 invokes it as void so the .catch in converted.ts:70/125 fires only on IPC transport errors, never a real reveal failure. Sibling open_file (commands.rs:462-466) and copy_file (340-348) return Result<(),String> and do surface errors â€” confirmed inconsistency: revealing a moved/deleted/disconnected output silently no-ops with no toast.

### [S3~] No long-path (>260) / verbatim-path handling for Windows output and split-folder writes

- **Evidence:** All output paths are built by naive PathBuf::join/format! (plan.rs:511-552 expected_output / expected_part_folder / expected_part_outputs) with no \\?\ verbatim prefixing; grep over src-tauri shows zero long-path handling (only the transitive `dunce` dep in Cargo.lock, unused by tamp code). A split adds a whole extra folder level ('{stem} (tamped {name} {hash})/{stem} {i}.{ext}') plus a '.part' temp sibling (part_path, plan.rs:559), and the readable preset label can add up to MAX_NAME_LEN=24 chars (plan.rs:473). A recording already near MAX_PATH (deep OneDrive/synced tree) can push the output, or especially the split part folder, past 260 chars. ffmpeg writing the .part file then fails; promote_part (mod.rs:1002) surfaces a generic 'cannot move finished output into place' / 'encoded output missing'.
- **Impact:** Recordings in deeply-nested synced folders fail to compress (or split) on default Windows with a confusing low-level error, even though the source file itself opened fine â€” and the user has no clue the path length is the cause.
- **Proposed fix:** Canonicalize/prefix Windows output and temp paths with the \\?\ verbatim form (or use the dunce crate already in the tree) before writing, and/or detect the over-length case and emit an actionable 'output path too long' error suggesting a shorter folder.
- **Decision needed:** Do we target machines without the Win10+ LongPathsEnabled opt-in (still the default-off majority), and is verbatim-prefixing acceptable given ffmpeg's own path handling?
- **Verifier:** Verified: output paths built by naive PathBuf::join/format! (plan.rs:511-552) with zero \\?\ verbatim handling (grep confirms only the MAX_PATH comment at plan.rs:472); split adds a folder level + 24-char label (plan.rs:473,525-552) + .part sibling (plan.rs:559-565), and failures surface as generic 'cannot prepare output folder' (mod.rs:638-642) or 'cannot move finished output into place' (mod.rs:1011). Real edge case, but the ffmpeg stderr tail IS surfaced in the row (mod.rs:1804-1808) so the error is somewhat less opaque than claimed, and triggering depends on runtime state (an input already near MAX_PATH on a non-long-path-aware volume). S3 is right.

### [S3] Non-UTF-8 file paths are lossily mangled before the encoder/clipboard ever see them, breaking the "reject, don't mangle" guarantee

- **Evidence:** scanner.rs:116 builds RecentVideo.path with path.to_string_lossy().into_owned(); the same lossy conversion happens for journal records (encoder/mod.rs:1289) and JobState (encoder/mod.rs:165). That lossy String is what the frontend round-trips back into enqueue/copy_file/reveal, where it becomes PathBuf::from(path) (commands.rs:218,245). By the time platform/mod.rs:77 paths_to_utf8 runs its "reject (rather than mangle) the exotic ones" check, the string already contains U+FFFD replacement chars, so the check never fires and the PathBuf no longer points at the real file. A Windows file with an unpaired surrogate (or any non-UTF-8 byte sequence on macOS) silently fails to open/probe/copy, or worse resolves to a different file.
- **Impact:** A recording whose name isn't valid UTF-8 either never compresses (ffprobe "no such file"), or its clipboard-copy/reveal/trash acts on the wrong/nonexistent path. The user sees a cryptic failure with no hint that the filename is the cause â€” the exact opposite of the comment's promise that the error 'names the offending file'.
- **Proposed fix:** Carry an opaque path identity (index into a backend-held Vec<PathBuf>, or a percent-encoded round-trippable token) instead of a lossy String, OR reject non-UTF-8 paths at scan time and surface them as a distinct "unsupported filename" row. At minimum, fix the paths_to_utf8 doc to stop claiming a guarantee that's already lost upstream.
- **Decision needed:** Do we commit to supporting non-UTF-8 recording filenames at all, or explicitly declare them unsupported and skip+warn them in the recents list with a visible reason?
- **Verifier:** Verified: scanner.rs:116, encoder/mod.rs:165 & 1289 all use to_string_lossy().into_owned(), and the frontend round-trips those strings back via PathBuf::from(path)/Path::new(&path) in commands.rs:245,344,353. The paths_to_utf8 guard (platform/mod.rs:77-88) does p.to_str() on those already-laundered PathBufs, so the U+FFFD replacement chars are valid UTF-8 and the "reject don't mangle" check can never fire â€” non-UTF-8 filenames get mangled before any reject, so probe/copy/reveal acts on a wrong/nonexistent path. Mechanism is exactly as described.

### [S3] Quick-compress from the file manager is Windows-only; macOS has no Finder Service / Quick Action equivalent

- **Evidence:** The 'Compress with tamp' right-click entry exists solely as a Windows HKCU registry registration (platform/context_menu.rs, gated #[cfg(target_os = "windows")] in lib.rs:234 and commands.rs:398). The Preferences toggle is hidden off-Windows (preferences.ts:520 if (isWindows())). MacOs::configure_app (platform/macos.rs:17) registers no Finder Services / Quick Action, and there is no .workflow or Info.plist NSServices entry. The CLI path (compress_file_args) works on macOS but nothing wires it to Finder.
- **Impact:** A core advertised workflow â€” right-click a recording to compress it â€” is entirely absent on macOS. A macOS user who reads about the feature (or switches from Windows) finds no way to trigger it from Finder, an obvious parity gap for an app whose whole identity is 'compress this recording fast'.
- **Proposed fix:** Add a macOS Finder Quick Action / NSServices 'Compress with tamp' entry that invokes the existing CLI/single-instance file-arg path, or explicitly document the feature as Windows-only and label it as such in marketing/help.
- **Decision needed:** Is a Finder Service/Quick Action in scope for this release, or do we ship Windows-only context-menu integration and accept the macOS gap for now?
- **Verifier:** Code matches exactly: context_menu module is HKCU-only (platform/mod.rs:9-10 cfg windows), startup apply gated at lib.rs:234, set_context_menu registry call gated at commands.rs:398, toggle hidden off-Windows at preferences.ts:520, and MacOs::configure_app (macos.rs:17) registers only the Accessory policy â€” no NSServices in tauri.macos.conf.json and no .workflow anywhere. compress_file_args (lib.rs:80) is platform-neutral and CLI-reachable but nothing wires it to Finder. The repo's own docs flag this as a known follow-up (drag-and-drop-design.md:178). Real parity gap; downgraded to S3 since macOS quick-compress still works via drag&drop/picker/CLI (Finder right-click is one of several entry points, not a broken core path), and it's an acknowledged, not-prominently-advertised TODO.

### [S3] Uninstaller and rebrand leave stale launch-at-login entries behind

- **Evidence:** nsis-hooks.nsh removes only the six context-menu registry keys on uninstall; it never deletes the autostart Run value the autostart plugin writes, so an uninstall with 'Launch at login' enabled leaves an HKCU\...\Run\tamp entry pointing at a now-deleted exe. Separately, lib.rs:181-187 re-enables autostart under the NEW identifier on a rebranded install but never disables the pre-rebrand LEGACY_IDENTIFIER (com.joystudios.tamp) LaunchAgent on macOS, where the plist is named by bundle id (com.joystudios.tamp.plist) and is thus orphaned rather than overwritten.
- **Impact:** After uninstall, Windows tries to launch a missing exe at every login (error popup / wasted entry). After the macOS rebrand, two launch agents can both be present, so the old bundle-id agent may relaunch the stale/old app or error at login.
- **Proposed fix:** Add an autostart Run-value DeleteRegValue to NSIS_HOOK_PREUNINSTALL; on startup, best-effort disable/remove the legacy-identifier LaunchAgent plist (mirror the migrate_legacy_data cleanup) before re-enabling under the new id.
- **Decision needed:** Should uninstall always strip autostart, or only when the user didn't choose 'keep my settings'? And do we proactively delete the legacy macOS LaunchAgent for everyone, or only when launch_at_login was on?
- **Verifier:** Windows half CONFIRMED: autostart writes HKCU\...\Run\tamp (auto-launch windows.rs:6,40 with app_name=package name "tamp", tamp passes None at lib.rs:153) plus a StartupApproved\Run value (windows.rs:8); nsis-hooks.nsh:3-8 deletes only the 6 context-menu keys, never the Run value, so uninstall-with-launch-at-login leaves a stale Run entry pointing at the removed exe. macOS half REFUTED: the plist is named {app_name}.plist with Label=app_name (auto-launch macos.rs:178-180,99) where app_name="tamp" (the product name, NOT the bundle id), unchanged across the rebrand, so lib.rs:184 overwrites the same ~/Library/LaunchAgents/tamp.plist rather than orphaning a com.joystudios.tamp.plist. Severity S3 stands on the real Windows defect (Windows silently ignores a missing Run target, so no error popup -> not S2).

### [S4~] Default Desktop asset-protocol scope ($HOME/Desktop) doesn't match a relocated/OneDrive Desktop on Windows

- **Evidence:** tauri.conf.json:35 hardcodes the static asset scope as ["$HOME/Desktop/**", "$VIDEO/**", "$APPCACHE/**"], but the Windows default watched folder comes from path.desktop_dir() (platform/windows.rs:107), which resolves the real Desktop known-folder â€” commonly redirected to %USERPROFILE%\OneDrive\Desktop. The two only coincide on a non-redirected profile. Startup compensates by scope.allow_directory'ing each watched folder (lib.rs:189-194) and enqueue widens scope per-file (commands.rs:199 dir_to_allow), so this isn't fatal, but the declared static scope is effectively dead on redirected Windows profiles and the design leans entirely on the runtime allow-list.
- **Impact:** Thumbnails/previews for desktop recordings rely solely on the runtime allow_directory call succeeding; if that ever logs a failure (lib.rs:191) the static scope won't back it up, and the mismatch is a latent footgun for anyone reasoning about the security scope. On a redirected profile the $HOME/Desktop entry protects nothing real.
- **Proposed fix:** Drop the OS-specific assumption from the static scope and rely on (well-tested) runtime allow_directory for watched folders, or add the redirected-Desktop case; document that thumbnail access is granted at runtime, not by the static scope.
- **Decision needed:** Should the static asset scope be trimmed to just $APPCACHE (cache is the only truly-static location) and let all media access be granted dynamically per watched folder, tightening the default scope?
- **Verifier:** Citations accurate: tauri.conf.json:35 hardcodes "$HOME/Desktop/**" (a literal join, not the Desktop known-folder), while platform/windows.rs:107 uses path.desktop_dir() which on OneDrive-redirected profiles returns %USERPROFILE%\OneDrive\Desktop. Runtime allow_directory (lib.rs:191) and dir_to_allow widening (commands.rs:218) are the real load-bearing protections and work regardless, so non-fatal; whether redirection actually occurs is runtime profile state. S4 is correct.

## Performance & resources

### [S2] First paint of the Videos list blocks on synchronous thumbnail + duration generation for the entire list (up to 200 videos)

- **Evidence:** src-tauri/src/commands.rs:44-45 â€” list_recents() awaits `ensure_thumbs(&app, &mut videos).await` and then `durations::fill(&app, &mut videos).await` before returning the Vec to the frontend. ensure_thumbs (thumbs.rs:124-162) spawns one ffmpeg-per-miss at MAX_CONCURRENT=4, and only after that completes does fill() (durations.rs:125-186) spawn one ffprobe-per-miss at MAX_CONCURRENT=4. The two phases run sequentially, not interleaved. With recentsLimit allowed up to 200 (settings.rs:367), a cold cache means the frontend's `await listRecents()` (list.ts:235) â€” and therefore the entire first render() â€” is gated on ~200 ffmpeg spawns followed by ~200 ffprobe spawns, 4 at a time. The list shows nothing (not even rows with placeholder thumbs) until every probe finishes.
- **Impact:** On first launch, after a cache wipe, or after the user bumps 'Recent videos shown' to a large value, the panel appears frozen/empty for many seconds (potentially 30s+ on a folder of 200 long recordings) every time it's opened from the tray, because the data the rows are built from isn't returned until all media work is done. A menu-bar app is expected to pop instantly.
- **Proposed fix:** Return the scanned rows immediately with thumb_path/duration_secs = None, render rows with placeholders, and stream thumbnails/durations in as separate events (or per-row lazy IPC like the Converted tab already does with conversion_thumb). At minimum, only generate thumbnails for rows near the viewport rather than the whole limit.
- **Decision needed:** Is instant-open with progressively-filling thumbnails acceptable product-wise, or must rows show their final thumbnail on first paint (which forces the blocking behavior)?
- **Verifier:** commands.rs:44-45 awaits ensure_thumbs then durations::fill sequentially before returning; both spawn ffmpeg/ffprobe-per-miss at MAX_CONCURRENT=4 (thumbs.rs:143, durations.rs:156); scanner truncates to recents_limit â‰¤200 (scanner.rs:128, settings.rs:367); list.ts:235-249 awaits listRecents() before render() with no streaming path, so a cold cache shows an empty panel until every probe finishes. Warm cache is fine (both pending vecs empty, fill early-returns durations.rs:140), so this is genuinely cold-cache/first-launch/bumped-limit scoped as stated. S2 is correct.

### [S3] ffprobe runs redundantly for the same file across the duration cache, preview generation, and the encode â€” no shared probe result

- **Evidence:** There is no shared probe cache. durations::fill (durations.rs:163-171) runs probe::probe() per uncached video to get duration. previews::generate (previews.rs:126) runs probe::probe() again for the same file when a preview is built. run_job (encoder/mod.rs:362,429) runs probe::probe() yet again at encode time. probe::probe (probe.rs:12-64) already extracts duration, width, height, fps, has_audio in one call, but only durations.rs persists the duration (as a bare f64, durations.json) â€” the rest is recomputed every time. For WebMs with no container duration, probe even falls back to a full packet-timestamp demux (probe.rs:75-99), making each redundant probe materially expensive.
- **Impact:** Opening the panel, then hovering to preview, then converting a single recording spawns three+ ffprobe processes against the same file (the duration one already paid for). For a no-duration WebM this is three packet-scan demuxes. Wasted process spawns and CPU, slower preview/encode start.
- **Proposed fix:** Introduce a small in-memory ProbeInfo cache keyed by the same path|mtime|size key the duration cache uses, populated once and consumed by durations, previews, and the encoder's reuse-short-circuit path.
- **Decision needed:** Is the duration cache worth widening into a full ProbeInfo cache (geometry can change only if the file changes, which the cache key already captures), or is the extra memory/complexity not justified?
- **Verifier:** Three live probe::probe call sites (durations.rs:164, encoder/mod.rs:362/429, previews.rs:126) all re-run the same ffprobe; ProbeInfo is never cached and only durations.rs persists a bare duration f64, so width/height/fps/has_audio (and the duration already paid for) are recomputed every time. No-duration WebMs re-trigger the packet-scan fallback (probe.rs:75-99) each time. Wasted process spawns/CPU only â€” no correctness impact â€” so S3 is right.

### [S3] The conversion journal is fully serialized and rewritten to disk on every single append, on the encoder worker thread

- **Evidence:** journal.rs:83-91 append() pushes one record then immediately calls persist(&records), which serde_json::to_vec's the ENTIRE Vec (up to MAX_RECORDS=200) and does a blocking std::fs::write of the whole file (journal.rs:108-126). append() is called synchronously from append_journal() inside the encode worker (encoder/mod.rs:557 single output, and encoder/mod.rs:830 once PER PART inside encode_part_set). A split into e.g. 10 parts therefore rewrites the entire 200-record JSON file 10 times in a row, each a full re-serialize + full-file write, blocking the worker between parts.
- **Impact:** O(n) full-file rewrites for what should be O(1) appends; for split jobs it's O(parts) full rewrites. Worst case is brief I/O stalls on the encode worker and unnecessary disk write amplification (rewriting ~50KB per part on a 10-part job). Not catastrophic, but not how a careful app persists an append-only log.
- **Proposed fix:** Append to the file incrementally (e.g. JSONL with O(1) appends + periodic compaction to MAX_RECORDS), or at least debounce persistence (write once after a job's full set completes rather than per part), and move the blocking write off the worker via spawn_blocking.
- **Decision needed:** Is keeping the human-readable single-array JSON format a hard requirement, or can the journal move to an append-friendly on-disk format?
- **Verifier:** journal.rs:90 append() calls persist() every time; persist() (108-126) serde_json::to_vec's the whole Vec (cap 200) + blocking std::fs::write of the full file. encoder/mod.rs:830 calls append_journal inside the per-part loop (731), so a 10-part split = 10 full re-serialize+full-file rewrites on the encode worker. Real as described; modest impact since each write is interleaved between ffmpeg part encodes that dominate wall time, so S3 is right.

### [S3] Converted tab re-runs conversion_thumb (IPC + fs stat, possibly ffmpeg) for every row on every refresh, and refreshes on every single 'done' event

- **Evidence:** converted.ts:351-373 refresh() does `scroll.innerHTML = ""` then rebuilds every row; each row calls thumb(outputPath) (converted.ts:88-99) which fires a fresh `conversionThumb(outputPath)` IPC call. There is no DOM/img reuse across refreshes and no in-page cache of resolved thumb paths. main.ts:109-112 calls `convertedView.refresh()` on EVERY encode:state event whose phase==='done' while the tab is open. During a batch (e.g. dropping 20 files, or a split job that appends a journal record per part), each completion tears down and rebuilds the whole list and re-issues conversion_thumb for every visible row. Each conversion_thumb (commands.rs:471-475 â†’ thumbs::ensure_one â†’ thumb_out) does an fs::metadata for the cache key plus an is_valid_thumb stat even on a hit.
- **Impact:** With a few dozen history rows, finishing a batch causes repeated full-list rebuilds, each spraying N backend IPC calls and 2N filesystem stats â€” visible flicker, scroll-position loss, and CPU churn precisely while the machine is already busy encoding. Feels janky and amateur.
- **Proposed fix:** Cache resolved thumbnail paths per outputPath in the view (or have the backend return thumb paths inside ConversionRecord), reuse existing DOM rows on refresh instead of innerHTML-clearing, and debounce/coalesce the done-driven refresh (refresh once after a short idle, not per event).
- **Decision needed:** Should the Converted history thumbnail be precomputed/stored in the journal record at encode time so the tab never needs per-row ffmpeg/IPC at all?
- **Verifier:** refresh() does scroll.innerHTML="" + full rebuild with no img/cache reuse (converted.ts:359,368-372); each row's thumb() fires conversionThumb IPC (converted.ts:93); main.ts:110-112 refreshes on EVERY phase==="done" event; compressPaths enqueues N separate jobs (list.ts:663,771) each emitting one Done (mod.rs:558-565), so dropping N files = N full rebuilds; ensure_one on a cache HIT still does 2 fs::metadata (thumbs.rs:105 thumb_out + thumbs.rs:29 is_valid_thumb). setSelected scrollIntoView (converted.ts:310) loses scroll/selection each time. One inaccuracy: a split job emits ONE Done not one-per-part (mod.rs:662-669), so no per-part storm. Real O(NÃ—rows) churn + flicker, but only while the Converted tab is the active visible tab â€” usually not during a drop (Videos tab shows progress), so S3 not S2.

### [S4~] ensure_thumbs and durations::fill hold up to 200 PathBufs/keys in 'pending' and apply results by index into a Vec that callers could resize

- **Evidence:** thumbs.rs:131-161 and durations.rs:131-181 both collect a `pending` Vec of (idx, ...) then spawn JoinSet tasks that report back done_idx and apply() indexes back into the original `videos` slice by position (thumbs.rs:81-85, durations.rs:145-152). This is correct only because the slice is never reordered between collection and application. It's fragile coupling: the index-based apply assumes the videos slice is stable across the whole async generation window. There's no bug today (list_recents holds the Vec and doesn't mutate ordering mid-await), but the two cache fillers silently depend on that invariant with nothing enforcing it.
- **Impact:** No user-facing impact today; this is a latent correctness/maintenance hazard â€” a future change that filters or sorts `videos` between scan and ensure_thumbs would misattach thumbnails/durations to the wrong rows with no error.
- **Proposed fix:** Key results by the stable cache_key (or path) rather than positional index when applying back, so the apply step is order-independent.
- **Decision needed:** Is it worth hardening these now, or is the 'list_recents owns the Vec and never reorders' invariant considered stable enough to leave as-is?
- **Verifier:** Code matches the description: thumbs.rs:81-85 apply() and durations.rs:145-152 apply() scatter task results back via videos.get_mut(idx), and the sole caller commands.rs:44-45 doesn't reorder videos between ensure_thumbs/fill. But there's no bug today (finder agrees): the &mut [RecentVideo] borrow is held exclusively across each whole async fn, so nothing can reorder mid-await, and get_mut() is bounds-checked (silent no-op, never a panic). Index-scatter-gather into a borrowed slice is idiomatic; the "future sort/filter mid-await" hazard requires a deliberate restructure that drops/reacquires the borrow. A mild maintainability note, correctly rated S4.

### [S4~] videoListSignature JSON.stringifies the full video array on every refresh, including panel:shown and tab switches

- **Evidence:** list.ts:70-82 videoListSignature() builds a 7-field tuple array over every video and JSON.stringify's it; refresh() (list.ts:238) computes it on every call to compare against lastSignature. refresh() runs on every tab switch to Videos (main.ts:83), every panel:shown event (main.ts:102-105), and after the initial load. For 200 videos this stringifies a 1400-element structure each time the user merely re-opens the panel from the tray.
- **Impact:** Redundant CPU on a hot path (panel open is the most frequent user action). Small in absolute terms but it runs on the exact interaction that must feel instant, and it allocates a multi-KB string each time only to usually discover nothing changed.
- **Proposed fix:** Track a cheaper signature (e.g. a running hash of path+mtime+size accumulated during scan, or compare length + a few summary fields first) instead of stringifying the whole list on every panel open.
- **Decision needed:** Is the signature comparison even needed on panel:shown, or can the backend tell the frontend whether the watched folders actually changed since last scan?
- **Verifier:** Facts verified: videoListSignature (list.ts:70-82) JSON.stringifies a 7-tuple/video array; refresh() computes it every call (list.ts:238) on tab-switch (main.ts:83) and panel:shown (main.ts:103). But this stringify IS the cheap guard that skips the expensive full render() (list.ts:239-249, preserving open previews), and it sits behind a two-command IPC round-trip (await Promise.all([listRecents(), queueState()]), list.ts:235) that dominates cost by orders of magnitude. A few-KB sub-millisecond stringify is noise next to the IPC+FS it gates. Real but cosmetic; S4 is correct/generous.

### [S4] Per-session preview proxy map and Converted-tab tooltip nodes grow without bound; no virtualization for up to 200 rows

- **Evidence:** list.ts:140 previewPaths Map accumulates one entry per video ever expanded and is never pruned for the session (only cleared on full render()). The disk cache is capped at MAX_CACHED=40 (previews.rs:14), so previewPaths can hold paths to montages the backend has already pruned, but that's only stale strings. More concretely, the Videos list builds a full DOM row per video with no virtualization (list.ts:495 `for (const v of videos) listScroll.appendChild(buildRow(v))`), and the Converted tab does the same (converted.ts:368-369); with recentsLimit/MAX_RECORDS at 200 that's up to 200 rows each carrying an <img>, several buttons, and (Converted) a body-appended tooltip wired on mouseenter (converted.ts:146-152). All 200 thumbnails decode and stay in the DOM at once.
- **Impact:** At the 200-row ceiling the panel holds ~200 decoded thumbnail bitmaps plus hundreds of event listeners in memory continuously, and layout/scroll cost rises noticeably. Mostly an issue only for power users at the high end of recentsLimit, but it's the kind of unbounded growth a senior reviewer flags.
- **Proposed fix:** Virtualize both lists (render only rows near the viewport, lazy-load thumbnails via IntersectionObserver as the Converted tab partially already does), and prune previewPaths against the backend cache cap.
- **Decision needed:** What's the realistic upper bound of recents users will set â€” is 200 a corner case worth virtualization, or should the limit simply be capped lower (e.g. 50) to sidestep it?
- **Verifier:** No virtualization in either tab: list.ts:495 and converted.ts:368-369 build one full DOM row per record; Videos thumbs eagerly set img.src (list.ts:1006) and Converted thumbs too (converted.ts:88-99), none with loading="lazy". recents_limit bounded 1..=200 (settings.rs:367), journal MAX_RECORDS=200 (journal.rs:14), so up to 200 eager-decoded thumbnails + per-row tooltip nodes/listeners (converted.ts:146-152) live at once. previewPaths (list.ts:140/909) does grow unpruned per session but holds only proxy-path strings (negligible), as the finder concedes. Real but modest; default limit is 50, so only power users at the ceiling are affected â€” S4 is correctly calibrated.

## Releasability & distribution

### [S3] Beta releases require a hand bump + later hand revert of package.json â€” a fragile manual dance that corrupts the next stable bump if missed

- **Evidence:** docs/releasing.md:47-59 documents the only beta path: step 1 manually edits package.json to e.g. `0.3.0-beta.1` and commits; step 4 says 'Before merging the branch, revert the version-bump commit â€” changesets computes the stable version from package.json, and a leftover -beta.N would corrupt the next bump.' prerelease.yml triggers purely on a `v*-beta*` tag (prerelease.yml:6-7) and never touches package.json, so nothing automates or verifies the revert. There is no guard in CI that fails if a `-beta` version reaches main.
- **Impact:** A forgotten revert silently poisons the next changesets version computation (the stable release inherits a -beta suffix or an off-by-one bump). It's a known-hazardous manual step the maintainer flagged themselves â€” exactly the kind of release-hygiene landmine a senior reviewer distrusts.
- **Proposed fix:** Drive beta versioning from the tag instead of editing package.json (derive the version from $GITHUB_REF_NAME at build time and pass it to tauri build / a temp manifest patch), so nothing needs reverting. Failing that, add a CI gate on main/PRs that rejects any package.json version containing '-beta'.
- **Decision needed:** Is the manual beta bump/revert acceptable maintainer overhead, or should betas be fully tag-driven so package.json on a release branch never carries a -beta suffix?
- **Verifier:** All cited facts hold: docs/releasing.md:52-59 prescribes hand-editing package.json to -beta.N then manually reverting before merge; prerelease.yml:6-7 triggers only on v*-beta* tags and never touches package.json; release.yml:33-40 derives the stable version from package.json via changesets (the documented source of truth), so a leftover -beta.N corrupts the next bump; grep confirms no CI guard fails a -beta version reaching main. A self-flagged manual landmine, but it's a documented "any branch" fallback gated on the maintainer forgetting a revert, so S3 is correct.

### [S3] macOS bundle is ad-hoc signed with no notarization, hardened runtime, or entitlements â€” permanent Gatekeeper friction and a notarization dead-end

- **Evidence:** tauri.macos.conf.json:5-7 sets `"signingIdentity": "-"` (ad-hoc) and defines no `hardenedRuntime`, `entitlements`, or notarization config. release.yml/prerelease.yml have no `codesign`/`notarytool`/`xcrun stapler` steps and set no APPLE_* secrets. README.md:100-104 tells every user to right-clickâ†’Open or run `xattr -dr com.apple.quarantine` on first launch. The roadmap (README.md:181) lists 'Notarized/signed builds' as not-yet-done.
- **Impact:** Every macOS user hits a Gatekeeper block and must run a terminal command or do the right-click dance â€” a hard drop-off for non-technical users and a trust red flag. Worse, without hardenedRuntime+entitlements the bundle can't be notarized even if a Developer ID is added later, so the current config is a dead-end, not just 'unsigned for now'.
- **Proposed fix:** Add `hardenedRuntime: true` and the entitlements the bundled ffmpeg sidecar needs (e.g. com.apple.security.cs.allow-unsigned-executable-memory / disable-library-validation as required), wire a Developer ID signing + notarytool + stapler step into the release workflow behind APPLE_* secrets. Short term, at least pre-strip quarantine isn't possible on a download, so keep the README bypass but prioritize a real cert.
- **Decision needed:** Is the maintainer willing to pay for an Apple Developer Program membership ($99/yr) to notarize, and similarly an EV/OV cert for Windows SmartScreen â€” or is unsigned-with-bypass the accepted long-term posture?
- **Verifier:** All cites verified: tauri.macos.conf.json:7 `"signingIdentity":"-"` with no hardenedRuntime/entitlements; base tauri.conf.json has no signing config; release.yml/prerelease.yml do `bun tauri build` + `gh release upload` with zero codesign/notarytool/stapler/APPLE_* steps; README.md:100-104 tells users to xattr/right-click-Open; README.md:181 lists notarized builds as roadmap. Real and reproducible, but the gap is openly disclosed (README + beta notes + roadmap) and fixable via standard Tauri config/CI, so it's documented friction, not a hidden defect.

### [S3] No first-run onboarding: macOS Desktop/permission prompt and notification permission are never explained or primed

- **Evidence:** lib.rs setup (lib.rs:158-249) goes straight to loading settings, registering shortcuts, and building the tray â€” there is no first-run/welcome flow, no permission priming, and no 'where did the icon go' guidance in-app. Notification permission is requested lazily only the first time a shortcut fires (shortcuts.rs:79-95 `notify` calls `request_permission` inline), so the macOS notification prompt appears out of context (or, if denied, the staleness/error warnings silently never show â€” shortcuts.rs:92-94 just logs). On macOS the Desktop-access TCC prompt is mentioned only in the README (README.md:108-109), never surfaced in the app. The bundled default preset is a single 'Discord (10MB)' (settings.rs:236-247); a first-time user opening Preferences sees no guidance.
- **Impact:** First-run is a cold start: the tray icon is easy to miss (the README itself warns Windows hides it behind the overflow `^`, README.md:92-94), the macOS Desktop permission prompt is unexplained, and a user who reflexively denies the notification prompt permanently loses the stale-recording safety warning with no recovery path. New users churn before reaching the 'one click' payoff.
- **Proposed fix:** Add a one-time first-run panel (or toast) that: points out the tray icon location, explains/triggers the Desktop permission on macOS up front, and primes the notification permission with context before requesting. Surface notification-denied state in Preferences with a re-request/deep-link to system settings.
- **Decision needed:** What is the intended first-run experience â€” a dedicated welcome screen, an inline coach-mark on the empty panel, or nothing â€” and which permissions should be primed proactively vs lazily?
- **Verifier:** No first-run/onboarding code exists (grep: hits only in docs, not app); lib.rs:158-249 setup never primes permissions; notify() requests notif permission lazily and only log_warn!s if denied (shortcuts.rs:92-94), and the stale-recording warning is delivered solely via notify() (shortcuts.rs:138-146), so denying it silently kills that safety warning with no in-app recovery. README.md:92-94/108-109 are the only place the tray-overflow and macOS Desktop TCC prompt are explained. Real UX gap but polish-level; S3 correct.

### [S3] Uninstaller leaks the launch-at-login autostart entry, all app data, cache, logs, and journal

- **Evidence:** nsis-hooks.nsh:1-9 (the only uninstall logic) deletes ONLY the six context-menu HKCU keys. It never removes: (a) the autostart Run key the autostart plugin writes when launch_at_login is on (lib.rs:183-184 enables it; commands.rs:88-91 toggles it) â€” after uninstall this leaves a `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` entry pointing at a deleted exe; (b) app_data_dir (settings.json, conversions.json journal â€” journal.rs, durations.rs); (c) app_cache_dir/thumbs + /previews (lib.rs:195-205, thumbs.rs, previews.rs); (d) app_log_dir rotating logs (logging.rs). macOS has no uninstaller at all â€” drag-to-Trash orphans the LaunchAgent plist (still tries to launch a missing app every login) plus the same data/cache/log dirs.
- **Impact:** After uninstall, Windows users get a broken startup entry that fails silently every boot, plus orphaned MBs of thumbnails/previews/logs. macOS users get a login item that errors forever and leftover app data. This is the textbook 'amateur installer' smell a reviewer immediately flags, and the broken Run key can trip antivirus/cleanup-tool warnings.
- **Proposed fix:** In NSIS_HOOK_PREUNINSTALL also delete the autostart Run value and (behind a prompt) the app-data/cache/log dirs. On macOS, document a real uninstall (remove the LaunchAgent plist + ~/Library/Application Support|Caches|Logs/<bundle id>) or ship an uninstall helper. Consider disabling autostart on uninstall regardless.
- **Decision needed:** Should uninstall wipe user settings/history by default, prompt, or leave them (for reinstall continuity)? And who owns the macOS uninstall story given there is no installer there?
- **Verifier:** nsis-hooks.nsh:1-9 only has NSIS_HOOK_PREUNINSTALL deleting the 6 context-menu HKCU keys; no POSTUNINSTALL data cleanup. autostart plugin registered (lib.rs:151-154), enabled (lib.rs:183-184)/toggled (commands.rs:87-92) â€” on Windows it writes HKCU\...\CurrentVersion\Run, never removed by the hook. app_data_dir (journal.rs:45), app_cache_dir thumbs/previews/durations (lib.rs:195-205, thumbs.rs:89, previews.rs:70), app_log_dir (lib.rs:164) all orphaned. No macOS uninstaller exists; LaunchAgent plist + dirs orphaned. Real leak, but the dead Run entry fails silently (no boot error dialog) and orphaned cache is small â€” closer to S3.

### [S3] No auto-update mechanism at all â€” users are stranded on whatever build they first installed

- **Evidence:** grep for `updater|tauri-plugin-updater|createUpdaterArtifacts|endpoints|pubkey` across the whole repo (excluding node_modules) returns zero hits. src-tauri/Cargo.toml:21-43 lists every Tauri plugin (store, autostart, single-instance, positioner, dialog, global-shortcut, notification, opener) but NOT tauri-plugin-updater. tauri.conf.json has no `plugins.updater` block and the bundle emits no update artifacts/signatures (release.yml:50-55 uploads only the raw .dmg/.exe). There is also no in-app 'Check for updates' affordance (tray.rs:8-10 has only Open Logs + Quit; the README version sits passively at the bottom of Preferences).
- **Impact:** Every release is a manual re-download from GitHub Releases. Users never learn a new version exists, never get bugfixes/security patches, and on Windows must re-clear SmartScreen on every manual reinstall. For a menu-bar utility that lives resident in the tray, silent/assisted updates are table-stakes; their total absence reads as amateur and strands the install base.
- **Proposed fix:** Add tauri-plugin-updater with a GitHub-Releases `latest.json` endpoint, generate signed update artifacts in release.yml/prerelease.yml (the updater requires a signing keypair â€” TAURI_SIGNING_PRIVATE_KEY), and add a tray/Preferences 'Check for updates' item. At minimum, ship a lightweight startup version-check that pings the GitHub releases API and toasts when a newer tag exists.
- **Decision needed:** Is the distribution model intended to stay manual-download-from-GitHub forever, or should tamp adopt the Tauri updater (which also forces you to maintain a signing keypair and a stable update channel per arch/OS)?
- **Verifier:** Verified: grep for updater/pubkey/endpoints returns zero; Cargo.toml:26-41 has no tauri-plugin-updater; tauri.conf.json has no plugins/updater block; release.yml:71-75 uploads only raw dmg/-setup.exe (no .sig/update artifacts); tray.rs:8-10 has only Open Logs+Quit; preferences.ts:133-137 shows a passive version line; docs/releasing.md describes a purely manual GitHub-Release download model. No auto-update exists. Downgrading to S3: the app is fully functional as installed and missing auto-update is a deliberate, common trade-off for small tray utilities, not a core-functionality break or failure path.

### [S4] Zero error reporting / telemetry and no crash visibility â€” failures only land in a local rotating log the user must find

- **Evidence:** README.md:81 advertises 'No uploads, no telemetry, no account' as a feature, and indeed there is no crash/error reporting anywhere: errors are written to app_log_dir via the log_* macros (logging.rs) and the only way to retrieve them is the tray's 'Open Logs' (tray.rs:8,43-58). A panic in setup is `.expect("error while building tamp")` (lib.rs:297) which just crashes with no surfaced report. Encode failures, settings-load corruption (settings.rs:287-291 backs up and silently falls back to defaults), and shortcut-registration failures (lib.rs:230-232) are logged-and-swallowed with no user-visible signal beyond an occasional toast.
- **Impact:** When something breaks in the field the maintainer has no signal at all â€” no aggregate crash rate, no opt-in diagnostics â€” and the user has to know to right-clickâ†’Open Logs and file an issue with attachments. Silent fallback-to-defaults on a corrupt settings file (a .bak is written but the user is never told their settings reset) is especially confusing.
- **Proposed fix:** Keep privacy-by-default but add an opt-in diagnostics path: a 'Report a problem' tray/Preferences item that bundles the logs and opens a prefilled GitHub issue, and a user-visible toast when settings failed to load and were reset (pointing at the .bak). Optionally an opt-in crash reporter (Sentry-style) gated behind explicit consent.
- **Decision needed:** Does the privacy stance forbid even opt-in crash/error reporting, or is an explicitly-consented diagnostics channel acceptable to get field signal?
- **Verifier:** All claims verified: no telemetry/crash-reporting or HTTP deps in src-tauri; errors only hit logging.rs rotating log reachable solely via tray "Open Logs" (tray.rs:8,43-58); setup panics with .expect("error while building tamp") (lib.rs:297); shortcut reg failure is log-and-swallow (lib.rs:230-232, shortcuts.rs:78); corrupt settings write a .bak and silently fall back to defaults with only a log line, no UI signal (settings.rs:287-291, backup_store_file 302-317). The no-telemetry part is an intentional advertised privacy feature (README:82), so the only true defect is the silent settings-reset â€” rare and data-preserving. S4 is correct.

### [S4] Beta prerelease workflow swallows GitHub-release-create failures, so builds can upload to nothing or a stale release

- **Evidence:** prerelease.yml:21-25 runs `gh release create "$GITHUB_REF_NAME" --prerelease ... || echo "release already exists"`. The `|| echo` masks ALL failures of `gh release create` â€” not just the 'already exists' case but auth errors, malformed notes, API outages, or a tag mismatch â€” turning every failure into a green step. The downstream build job (prerelease.yml:55-58) then runs `gh release upload "$GITHUB_REF_NAME" ... --clobber`, which would fail or attach assets to the wrong/old release. The notes are also hardcoded to mention only 'Gatekeeper/SmartScreen bypass' generically with no changelog.
- **Impact:** A genuinely failed release-create looks successful, and the asset upload either errors confusingly or clobbers a pre-existing same-named release's assets. Debugging a botched beta becomes guesswork. The blanket `|| echo` is a code-smell a reviewer flags instantly.
- **Proposed fix:** Replace `|| echo` with an explicit check: query `gh release view` first and only create-if-absent, or use a `gh release create ... 2>&1 | grep -q 'already_exists'` guard that re-raises on other errors. Pull release notes from the changeset/CHANGELOG instead of a hardcoded string.
- **Decision needed:** Should a re-tagged beta (vX.Y.Z-beta.N re-pushed) reuse and clobber the existing prerelease, or always fail loudly so a human decides?
- **Verifier:** prerelease.yml:22-25 `gh release create ... || echo "release already exists"` masks every failure (auth/API/malformed-notes), not just the idempotent already-exists case, so the step is always green; downstream upload at :58 uses `--clobber`. Hardcoded notes (:24) carry no changelog. Real CI code-smell, but a genuine create failure with no existing release would actually surface as a red `gh release upload` (release-not-found), so "upload to nothing" is partly self-correcting â€” the live risk is silently clobbering a stale same-tag release; S4 is the right altitude for a CI-only robustness/polish issue.

### [S4] Cargo.toml / Cargo.lock pinned at 0.1.0 while the app version is 0.2.0 â€” stale and divergent version source

- **Evidence:** package.json:4 is `"version": "0.2.0"` and CHANGELOG.md:3 documents 0.2.0, but src-tauri/Cargo.toml:3 is `version = "0.1.0"` and Cargo.lock's `name = "tamp"` block is also `version = "0.1.0"`. tauri.conf.json:4 reads `"version": "../package.json"`, so the *bundle* is versioned correctly, but the Rust crate version (used in any `env!("CARGO_PKG_VERSION")`, panic/backtrace metadata, and anything reading the crate manifest) is frozen a full minor behind. docs/releasing.md:41 calls package.json 'the single source of version truth' but nothing keeps Cargo.toml in sync â€” changeset version only bumps package.json.
- **Impact:** Crate-level version is wrong and drifts further every release. Any future telemetry/error report, `cargo`-emitted metadata, or a contributor reading Cargo.toml sees the wrong version. It signals the version process isn't actually wired end-to-end.
- **Proposed fix:** Either remove the version from Cargo.toml's authority (have it read package.json the way tauri.conf does, or add a `bun run version` post-step that rewrites Cargo.toml/Cargo.lock from package.json), or document and enforce a single bump. Add a CI check that package.json and Cargo.toml versions match.
- **Decision needed:** Is package.json meant to be the sole version source (then Cargo.toml should be auto-synced and committed by the release PR), or should the release process bump both?
- **Verifier:** Cargo.toml:3 and Cargo.lock:3498 both pin tamp at 0.1.0 while package.json:4/CHANGELOG.md:3 are 0.2.0; releasing.md:40-42 only bumps package.json, so the crate drifts. But CARGO_PKG_VERSION/crate-version is consumed nowhere (grep: only env! uses are CARGO_MANIFEST_DIR & TAMP_TARGET_TRIPLE in encoder/bin.rs), so no functional/telemetry impact â€” purely hygiene. Drop to S4.

## Internationalization & content

### [S3] truncateMiddle slices by UTF-16 code units â€” splits emoji/surrogate pairs and is not width-aware for CJK

- **Evidence:** src/lib/format.ts:113-118 uses `s.length`, `s.slice(0, head)` and `s.slice(s.length - tail)`. These operate on UTF-16 code units. A filename containing an astral character (emoji, some CJK ext-B) has surrogate pairs; if `head` or `s.length - tail` lands between a pair, slice yields a lone surrogate that renders as the replacement character. Separately, CJK/full-width filenames count 1 code unit per glyph but render ~2 columns wide, so 36 "chars" of CJK overflows the row visually. Applied to raw user filenames at src/views/list.ts:1033 and src/views/custom.ts (truncateMiddle import).
- **Impact:** Recordings named with emoji or mixed scripts can show a stray  in the middle-ellipsis; CJK-named files truncate at double the intended visual width and overflow/clip. Both look broken to non-Latin users.
- **Proposed fix:** Truncate over an array of code points (`[...s]` / Intl.Segmenter grapheme segmenter) instead of code units; for width, optionally measure with a wcwidth-style cap. At minimum guard against splitting a surrogate pair.
- **Decision needed:** Which scripts/filename styles must look correct (emoji-only? CJK? RTL?) â€” does that justify pulling in Intl.Segmenter, or is a surrogate-safe slice enough?
- **Verifier:** format.ts:113-118 uses s.length/s.slice (UTF-16 code units) with no surrogate guard; head/tail boundaries can split an astral pair into a lone surrogate (renders U+FFFD). Reachable via list.ts:1025/1033 and custom.ts:60 on raw user filenames. CJK width: .row-name has white-space:nowrap;overflow:hidden but no text-overflow:ellipsis, so 36 CJK code units (~double width) hard-clip with the extension off-screen. Cosmetic, non-Latin-only, layout contained by CSS â€” S3 correct.

### [S3] Date/time formatting hardcodes 24-hour clock and English month names; tooltip mixes locale-aware and English formats

- **Evidence:** format.ts:7-10 hardcodes English `MONTHS`; formatClock (format.ts:37-42) and formatRelativeTime (45-62) always render 24h "HH:MM" with no AM/PM. formatAbsolute (65-79) uses locale-aware toLocaleDateString/toLocaleTimeString but forces `hour12: false`. In the Converted tooltip these two collide: the visible label uses English+24h formatRelativeTime ("Yesterday 14:03", "May 2 09:30") at converted.ts:134, while the same tooltip's Created/Converted rows use locale-aware formatAbsolute at converted.ts:141-142 â€” so a French/German/Japanese user sees their locale's month order in the tooltip body but English "May"/"Yesterday" and forced 24h in the label.
- **Impact:** en-US users (AM/PM expected) always see 24h; non-English users see English "Yesterday"/"May" relative labels mixed with their own locale's absolute dates in the same hover card â€” visibly inconsistent and clearly not internationalized.
- **Proposed fix:** Either commit to a single deliberate style everywhere (document the 24h/English choice as intentional and make the tooltip match), or route all of formatRelativeTime/formatClock through Intl.DateTimeFormat / Intl.RelativeTimeFormat so the whole app follows the OS locale and 12/24h preference consistently.
- **Decision needed:** Is a single fixed presentation locale (English, 24h) an explicit product decision, or should the app follow the OS locale and 12/24h setting?
- **Verifier:** Verified: format.ts:7-10 hardcodes English MONTHS; formatClock (37-42) and formatRelativeTime (45-62) emit literal "Just now"/"Yesterday"/MONTHS[] + 24h formatClock; formatAbsolute (65-79) is locale-aware via toLocaleDateString/TimeString(undefined) yet forces hour12:false. converted.ts:134 sets the visible label from formatRelativeTime (English+24h) while 141-142 fill Created/Converted rows from formatAbsolute (locale month order) â€” the two collide in one hover card. Real and code-grounded; cosmetic/i18n only, so S3 is correct.

### [S3] Group conversion badge renders ungrammatical "1 parts" â€” no singular/plural handling

- **Evidence:** src/views/converted.ts:261-262 sets `${node.parts.length} parts` with no pluralization. A group node is created by groupConversions (src/lib/convgroup.ts:48-56) for ANY folder that has at least one part-path record. While a fresh backend split always writes >1 parts (src-tauri/src/encoder/mod.rs:364 gates on `split.count > 1`), the journal is immutable history re-grouped by folder path: if all but one part file is deleted/re-compressed, or a single part record survives, the group still renders and shows the literal text "1 parts". The list.ts equivalent guards this with `j.part[1] > 1` (src/views/list.ts:1149-1151); converted.ts does not.
- **Impact:** A discerning user sees grammatically broken "1 parts" in the Converted tab â€” an obvious amateur tell. Inconsistent with list.ts which already pluralizes correctly.
- **Proposed fix:** Add a tiny pluralize helper (or `parts.length === 1 ? '1 part' : `${n} parts``) and use it everywhere a count is rendered. Better: collapse a 1-part group to a single row since it is not really a split.
- **Decision needed:** Should a 1-part group ever be shown as a group at all, or always rendered as a single conversion row?
- **Verifier:** converted.ts:261-262 hardcodes `${node.parts.length} parts` with no plural handling; groupConversions (convgroup.ts:48-58) emits a group node for ANY tamped folder with >=1 part-path record (no count>1 guard), so a journal with a single surviving part renders literal "1 parts" â€” unlike list.ts:1149-1151 which guards `j.part[1] > 1`. Fresh splits gate on count>1 (encoder/mod.rs:364), so the 1-part case is a journal-state edge (partial deletion/re-compress), making it real but uncommon â€” S3 correct.

### [S3] Raw lowercase technical Rust errors are shown verbatim to users via showToast(String(e))

- **Evidence:** ~15 call sites do `showToast(String(e))` / `showToast(String(err))` on ipc rejections (src/views/list.ts:251,527,557,739-740,990,1069; src/views/converted.ts:67,69-70,105,116,125,272,290,356; src/views/preferences.ts:86,532,649). The backend strings these surface are developer-facing and lowercase: "recents scan failed: {e}" (commands.rs:31), "clipboard task failed: {e}" (commands.rs:347), "couldn't open {path}: {e}" (commands.rs:466), "ffmpeg -encoders exited with {status}" (hw.rs:56), "failed to clear global shortcuts: {e}" (shortcuts.rs:31). None tell the user what to DO, several leak raw OS/ffmpeg internals and full file paths into a toast.
- **Impact:** Error tone is inconsistent (some sentences end with actionable guidance like the trash-conflict message, most are bare technical fragments). Users hit "clipboard task failed: JoinError..." or a wall of ffmpeg stderr with no next step â€” feels broken, not professional.
- **Proposed fix:** Introduce a thin error-mapping layer at the ipc boundary that maps known backend error kinds to short, sentence-case, actionable user messages ("Couldn't copy the file. Try again, or reveal it in your file manager."), keeping the raw text only in the title/tooltip or logs.
- **Decision needed:** Is there an accepted catalog of user-facing error copy, or should every command return a structured error code the frontend maps to localized copy?
- **Verifier:** All ~15 showToast(String(e)) sites verified (list.ts:251,527,557,740,990,1069; converted.ts:67,69,105,116,125,272,290,356; preferences.ts:86,532,649); toast.ts:11 sets textContent verbatim with no normalization; backend commands return Result<_,String> so String(e) is the raw lowercase fragment ("recents scan failed: {e}" commands.rs:31, "clipboard task failed: {e}" :347, "couldn't open {path}: {e}" :466, "ffmpeg -encoders exited with {status}" hw.rs:56). Tone inconsistency is real even within commands.rs (capitalized actionable "Couldn't update Launch at login" :126 / TRASH_MULTI_PRESET_ERR :242 vs bare fragments). Real but confined to uncommon failure paths and shows the user's own paths in their own toast (no security hole, no broken core flow) â€” S3, not S2.

### [S4] Entire UI string set is hardcoded English with no i18n layer or extraction point

- **Evidence:** All user-facing copy is inline literals across the frontend (e.g. "No videos in your watched folders yet â€” record something!" list.ts:490, "No conversions yet.\nâ€¦" converted.ts:363-364, "Filter recordingsâ€¦" list.ts:108, "ï¼‹ Add fileâ€¦" list.ts:114, "Copied to clipboard" repeated ~6x, keyboard-hint strings list.ts:785-786) and backend (TRASH_MULTI_PRESET_ERR commands.rs:11, give_up_error retry.rs:99, notify("No recent videos found") shortcuts.rs:128). There is no resource bundle, no string table, and no i18n dependency in the project.
- **Impact:** Localization is impossible without a refactor; acceptable for a v1 menubar app but worth an explicit decision. Also means duplicated literals ("Copied to clipboard", "Show in file manager") drift independently.
- **Proposed fix:** If localization is even a medium-term goal, centralize strings into a single module/dictionary now (cheap while the surface is small) so duplicates collapse and a future i18n layer has one seam; otherwise record the English-only decision explicitly.
- **Decision needed:** Is localization on the roadmap at all? If not, is de-duplicating repeated literals into one strings module still worth doing for consistency?
- **Verifier:** All citations verified: hardcoded English at list.ts:489-490/108/114, converted.ts:363-364, commands.rs:11 (TRASH_MULTI_PRESET_ERR), shortcuts.rs:128; "Copied to clipboard" duplicated 6x (list.ts:739, converted.ts:69/115/271/290, drawer.ts:104); package.json has no i18n/intl/localization dep. Accurate but it's an architectural/future-proofing note, not a functional defect â€” S4 is correct.

### [S4] Split "by seconds" summary always says "â‰ˆNs" with no max â€” large values read as raw seconds ("split â‰ˆ600s")

- **Evidence:** splitSummaryLabel (format.ts:107-109) returns `split â‰ˆ${split.seconds}s` for any seconds value. The input has min=10 but NO max (src/lib/forms.ts:171-173, only `secondsInput.min='10'`, no `.max`), and validation only requires `seconds >= 10` (forms.ts:105). A user can enter 300/600/3600 and the preset summary chip and the field label "â‰ˆ every N seconds" (forms.ts:176) both stay in raw seconds, e.g. "split â‰ˆ600s" instead of "â‰ˆ10m".
- **Impact:** Large by-duration splits show clumsy "â‰ˆ600s"/"â‰ˆ3600s" in the preset summary line â€” reads as unpolished; no human-friendly minute rollover.
- **Proposed fix:** Render the seconds value with a duration-aware formatter ("â‰ˆ10m", "â‰ˆ1h") once it exceeds 60/3600, mirroring formatDuration's tiers; optionally cap the input with a sensible max.
- **Decision needed:** What is the intended upper bound for by-duration splits, and should the summary roll seconds up into minutes/hours above a threshold?
- **Verifier:** format.ts:107-109 returns `split â‰ˆ${split.seconds}s` with no minute rollover; forms.ts:172 sets only secondsInput.min='10' (no .max, unlike partsInput.max='20' at 169) and forms.ts:105 validates only seconds>=10, so 600/3600 render raw as "split â‰ˆ600s". Real but pure cosmetic polish for uncommon large values; S4 correct.

### [S4] Byte and percent formatting ignore the user's locale decimal/grouping separators

- **Evidence:** formatBytes (format.ts:12-34) builds strings with `.toFixed()`/`.toString()` and a template literal, always producing a '.' decimal and no digit grouping ("1.2 MB", "229 MB"). formatPercentSmaller (format.ts:92-96) likewise uses `Math.round(...)` + template ("93.9% smaller"). A locale that uses ',' as the decimal separator (de, fr, es, pt, ...) would conventionally write "1,2 MB" / "93,9 %".
- **Impact:** Numbers don't match the conventions of comma-decimal locales â€” minor but noticeable to a discerning European user; reinforces the impression that no i18n exists.
- **Proposed fix:** Format numeric magnitudes with Intl.NumberFormat (maximumFractionDigits:1) when locale support is desired, keeping the unit token separate; or explicitly document English numeric formatting as an intentional fixed style.
- **Decision needed:** Should numeric formatting follow OS locale (Intl.NumberFormat) or stay deliberately en-style for screenshot/consistency reasons?
- **Verifier:** format.ts:14,26,33 (trimmed via toFixed/toString + template) and :95 (Math.round + `% smaller` template) emit locale-invariant '.' decimals with no grouping; no Intl.NumberFormat/toLocaleString for numbers anywhere in src, so comma-decimal locales get "1.2 MB"/"93.9% smaller". Real but cosmetic; whole UI is hardcoded English, so this is one facet of an intentional no-i18n design â€” S4 appropriate.

## Visual polish & theming

### [S2] Dropping a video while on the Converted/Preferences tab (quick-pick mode) shows nothing â€” the preset picker renders inside the hidden Videos view

- **Evidence:** Drag-drop is global (src/main.ts:97) and always routes to listView.compressPaths. In quick-pick mode that calls openQuickPickForPaths, which mounts the overlay with `el.appendChild(overlay)` (src/views/list.ts:730) where `el` is the Videos view. When another tab is active that view has [hidden], and src/styles.css:83 `[hidden]{display:none!important}` hides the overlay too. The drop-overlay (in document.body) disappears on drop, so the user sees no picker at all.
- **Impact:** A discerning user drops a file while viewing Converted or Preferences, the drop hint vanishes, and nothing visibly happens â€” the quick-pick menu is painted inside a display:none container. The drop appears to be silently ignored (only blind keyboard input would drive the invisible picker).
- **Proposed fix:** Mount the quick-pick overlay on the .panel (or document.body) like the drop overlay, independent of the Videos view's hidden state; or auto-switch to the Videos tab on drop before opening the picker.
- **Decision needed:** On an external drop while another tab is active, should we force-switch to the Videos tab, or keep the current tab and float the picker above it?
- **Verifier:** Verified: global drop (main.ts:97-98 â†’ dragdrop.ts:52) calls listView.compressPaths; in quick-pick mode (default, list.ts:564, shouldPickPreset:59) it runs openQuickPickForPaths which does el.appendChild(overlay) at list.ts:730, where el is the Videos view (list.ts:100). On Converted/Prefs tabs setTab sets listView.el.hidden=true (main.ts:75), so [hidden]{display:none!important} (styles.css:83) hides the picker. No tab-switch in compressPaths/openQuickPickForPaths, and the drop-overlay self-hides (dragdrop.ts:46) â€” user sees nothing. S2 correct.

### [S3] The activity drawer's scrollable list uses the default chunky scrollbar, not the app's slim themed one

- **Evidence:** src/styles.css:1463 `.drawer-rows { max-height:220px; overflow-y:auto }` but the custom slim 6px scrollbar rules at :232-252 only target .list-scroll, .view-prefs, .modal-body. .drawer-rows is omitted, so it falls back to the OS/webview default scrollbar.
- **Impact:** With several concurrent conversions the download-manager drawer shows a wide default scrollbar (light/heavy on Windows, mismatched in dark mode) right in the most visible 'app is working' surface â€” an obvious inconsistency next to the polished slim scrollbars elsewhere.
- **Proposed fix:** Add .drawer-rows to the shared ::-webkit-scrollbar selector group (or extract a reusable .scroll-slim class applied to all four scroll containers including .quickpick if it ever scrolls).
- **Decision needed:** Do we want one shared scroll-slim utility class applied everywhere, or keep enumerating selectors â€” and should the quick-pick overlay get the same treatment?
- **Verifier:** styles.css:232-252 scope slim 6px themed scrollbar to .list-scroll/.view-prefs/.modal-body only; no global ::-webkit-scrollbar rule and .drawer-rows (styles.css:1459-1465, overflow-y:auto, max-height:220px) is omitted, so the always-visible activity drawer (drawer.ts:30-31) overflowing at ~4+ rows falls back to the default webview scrollbar.

### [S3] .row-name hard-clips with no ellipsis, inconsistent with every other name field

- **Evidence:** src/styles.css:350 `.row-name { white-space:nowrap; overflow:hidden }` â€” missing text-overflow:ellipsis, unlike .preset-name (:704), .conv-name (:1166), .drawer-name (:1488) which all set it. The name column also holds an inline badge + reveal button (src/views/list.ts:1027-1036) eating width, and JS only middle-truncates to 36 chars (list.ts:1033), which can still overflow the narrow column.
- **Impact:** A long recording name in the Videos list gets sliced off mid-character with a hard edge instead of a graceful 'â€¦', and can clip the trailing reveal magnifier â€” looking cut off rather than intentionally truncated, out of step with the rest of the UI.
- **Proposed fix:** Add text-overflow:ellipsis to .row-name (and ensure the inline reveal button/badge sit outside the ellipsizing text span so they aren't clipped).
- **Decision needed:** Should the reveal icon and 'compressed' badge stay pinned/visible while only the filename text ellipsizes, or is clipping the whole line acceptable?
- **Verifier:** styles.css:350-356 `.row-name` sets `white-space:nowrap; overflow:hidden` with no `text-overflow:ellipsis`, while `.preset-name` (:704), `.conv-name` (:1168), `.drawer-name` (:1488) all set ellipsis â€” real inconsistency. truncateMiddle (format.ts:113-118) caps by char count (36), not pixel width, and list.ts:1019-1036 packs the text + inline badge + 16px+5px reveal button on one nowrap line inside flex `.row-info` (min-width:0), so a long name in a narrow tray column hard-clips mid-character instead of showing 'â€¦'. Cosmetic only (title tooltip at list.ts:1021 preserves full name); S3 is correct.

### [S3] .active-bar-name has no truncation; a long preset name wraps and breaks the bar layout

- **Evidence:** src/styles.css:1609 `.active-bar-name { min-width: 80px; text-align: center }` â€” no max-width, white-space:nowrap, overflow, or text-overflow. Preset names are free-text (src/views/preferences.ts:231 placeholder 'e.g. Slack (25MB)'). The active bar is flex space-between (src/styles.css:1582) with no fixed height.
- **Impact:** In active-bar mode a long preset name (e.g. 'Discord Nitro 4K 500MB') wraps to multiple lines, growing the bar vertically and vertically de-centering the â€¹ â€º arrows â€” a visibly broken, amateur-looking control.
- **Proposed fix:** Add white-space:nowrap; overflow:hidden; text-overflow:ellipsis and a max-width (or flex:1 min-width:0) to .active-bar-name so long names truncate with an ellipsis while the arrows stay put.
- **Decision needed:** Should the active-bar name truncate with an ellipsis (and rely on a title tooltip), or should very long preset names be length-capped at creation time instead?
- **Verifier:** styles.css:1609-1615 `.active-bar-name` has only `min-width:80px;text-align:center` â€” no max-width/white-space:nowrap/overflow/text-overflow. Name is set verbatim via `textContent = ap.name` (list.ts:623); preset name is free-text with no maxLength on the input (preferences.ts:231) and save only checks non-empty (preferences.ts:285-289). In active-bar flex row (styles.css:1582, no fixed height) a long name wraps and grows the bar vertically. Minor wording nit: align-items:center keeps arrows centered, so they aren't "de-centered," but the bar still visibly grows. S3 correct.

### [S3] Light-theme toggle switches are nearly invisible when OFF: white knob on a near-white track

- **Evidence:** src/styles.css:1056 `.switch .track { background: var(--border) }` and :1069 `.switch .track::after { background: #fff }`. In light theme --border is #e6e6ea (line 59). An OFF toggle paints a #fff knob on a #e6e6ea track with no border/shadow on the knob, so the thumb almost disappears and the control reads as a flat featureless pill.
- **Impact:** Every toggle in Behavior preferences (Copy to clipboard, Move to Trash, GPU encoder, Launch at login, Windows context-menu) looks ambiguous in light mode â€” users can't tell at a glance whether a setting is on or off, and an unchecked switch barely looks like a switch.
- **Proposed fix:** Give the OFF track more contrast (e.g. --surface-2/--border-strong) and/or add a subtle border or drop-shadow to the knob so the #fff thumb separates from the track in light mode. Consider a --switch-track / --switch-knob semantic var pair so both themes are tuned independently.
- **Decision needed:** Should the OFF state use a visibly darker track, or keep the light track but add a ring/shadow to the knob â€” which reads as more 'native' on Windows light mode?
- **Verifier:** styles.css:1056 OFF track=var(--border)=#e6e6ea (light, line 59) and :1069 knob=#fff with no box-shadow/border (only 4 .switch rules, no light override); ~6% luminance gap makes the OFF thumb nearly invisible. Real, but ON-state accent track stays clear and the control remains usable/clickable, so polish-level: S3.

### [S3] All toasts carry a hardcoded red 'error' border, including success confirmations

- **Evidence:** src/styles.css:1562 `.toast { border: 1px solid rgba(248,113,113,0.4) }` is the only toast style. src/lib/toast.ts:9 showToast has no variants, and it's called for successes too â€” e.g. "Copied to clipboard" at src/views/converted.ts:69/116 and src/views/list.ts:739.
- **Impact:** Copying a compressed file or any positive confirmation pops a toast framed in danger-red, signalling failure when the action actually succeeded. It makes routine success feedback look alarming and undermines trust in the status colors.
- **Proposed fix:** Add toast variants (e.g. showToast(msg, 'error'|'success'|'info')) and key the border/accent off --danger vs --success vs --border, or at minimum switch the default border to a neutral --border and only use the red border for error toasts.
- **Decision needed:** Do we want explicit success vs error toast styling (icon + color), or a single neutral toast â€” and which calls should be classified as errors vs confirmations?
- **Verifier:** Single .toast rule (styles.css:1555-1568) hardcodes a red border rgba(248,113,113,0.4); showToast (toast.ts:9) has no variant param and is reused for successes like "Copied to clipboard" (converted.ts:69/115/271/290, list.ts:739, drawer.ts:104), so confirmations render danger-red. Real but purely cosmetic â€” text is correct, no functional impact â€” so S3, not S2.

### [S4] No keyboard focus-visible styling on any button; tabbing relies on the raw webview outline that clashes with the theme

- **Evidence:** src/styles.css defines focus styling only for inputs (:882 `.input:focus`). No :focus-visible rule exists for .seg-btn, .chip, .btn-primary/.btn-ghost, .conv-act, .quickpick-item, .modal-back, .pin-btn, etc. (grep for focus-visible/outline returns only :873 outline:none on .input and the input:focus rule).
- **Impact:** Keyboard/accessibility users tabbing through the controls get the browser-default focus ring (and on some platforms none at all on transparent backgrounds), which neither matches the violet accent used for the row selection ring nor reads as intentional â€” inconsistent and a mild a11y gap for an otherwise keyboard-rich app.
- **Proposed fix:** Add a shared :focus-visible rule using the accent (e.g. box-shadow: 0 0 0 2px var(--accent) or outline matching the .row.is-selected ring) across interactive elements, themed via the existing accent vars.
- **Decision needed:** Should focus rings match the existing violet row-selection ring exactly for consistency, or use a distinct lighter ring so keyboard focus is distinguishable from arrow-key list selection?
- **Verifier:** styles.css has focus styling only for .input (:882-885 box-shadow ring), preceded by a scoped outline:none at :873. No :focus or :focus-visible exists for .seg-btn (148), .chip (627), .btn-primary (761), .btn-ghost (785), .conv-act (1189), .quickpick-item (1670), .modal-back (929), .pin-btn (1754) â€” all only have base/:hover/:disabled/.is-active rules. No global *:focus reset and no global outline rule exist, so these controls fall back to the default webview ring that doesn't match the violet accent. Real but cosmetic; S4 is correct.

### [S4] Quick-pick preset overlay has no max-height/scroll; many presets overflow and clip top and bottom

- **Evidence:** src/styles.css:1654 `.quickpick { max-width:320px; padding:8px }` has no max-height or overflow; the overlay centers it with align-items:center (:1649). Presets render one row each (src/views/list.ts:689) with Custom + a hint appended, and only items 1-9 get a number but all are listed.
- **Impact:** A user with many presets (10+) opens the picker and the list grows past the 640px panel; because it's vertically centered, both the first and last presets are clipped off-screen with no way to scroll to them.
- **Proposed fix:** Cap the quickpick with max-height (e.g. calc(100% - 36px)) and overflow-y:auto with the slim scrollbar, or switch the overlay to align-items:flex-start with top padding so overflow only spills downward and stays scrollable.
- **Decision needed:** Is there a practical cap on preset count, or should the picker always be scrollable to support arbitrarily many presets?
- **Verifier:** No max-height/overflow on .quickpick (styles.css:1654-1662) and overlay centers with align-items:center (:1647) in a 640px window (tauri.conf.json:20); presets are user-unbounded (preferences.ts:321 pushes with no cap) and each renders one ~34px row (list.ts:689-705), so a long list clips at both top and bottom unscrollable. Real but rare: overflow actually starts ~16 presets given card chrome, not 10, so S4 is right.

