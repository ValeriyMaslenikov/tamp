# CLAUDE.md

Agent guidance for this repo lives in **[AGENTS.md](AGENTS.md)** — one source of
truth shared by Claude Code and other agent tools. Read it first.

It covers the project layout, architecture rules (OS behavior behind the
`Platform` trait; the "(tamped …)" naming parsers kept in lockstep), the
build/verify commands, and the **release process**
([docs/releasing.md](docs/releasing.md)).
