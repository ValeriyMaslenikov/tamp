{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the tray panel window",
  "windows": ["panel"],
  "permissions": [
    "core:default"
  ]
}
