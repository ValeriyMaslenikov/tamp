## What

<!-- What does this PR change, and why? -->

## How was it tested?

<!-- bun run test / cargo test, manual steps in the app, screenshots for UI changes -->

## Checklist

- [ ] `bun run test` and `bun run build` pass
- [ ] `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test` pass (in `src-tauri/`)
- [ ] Added a changeset (`bun changeset`) if this changes user-facing behavior
